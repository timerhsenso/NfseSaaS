diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Application/UseCases/Empresas/AlterarAmbienteEmpresaRequest.cs NfseSaaS-master/src/NfseSaaS.Application/UseCases/Empresas/AlterarAmbienteEmpresaRequest.cs
--- a/src/NfseSaaS.Application/UseCases/Empresas/AlterarAmbienteEmpresaRequest.cs	1970-01-01 00:00:00.000000000 +0000
+++ b/src/NfseSaaS.Application/UseCases/Empresas/AlterarAmbienteEmpresaRequest.cs	2026-09-10 13:29:19.901491069 +0000
@@ -0,0 +1,5 @@
+using NfseSaaS.Domain.Enums;
+
+namespace NfseSaaS.Application.UseCases.Empresas;
+
+public sealed record AlterarAmbienteEmpresaRequest(TipoAmbiente TipoAmbiente);
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Application/UseCases/Empresas/AlterarAmbienteEmpresaRequestValidator.cs NfseSaaS-master/src/NfseSaaS.Application/UseCases/Empresas/AlterarAmbienteEmpresaRequestValidator.cs
--- a/src/NfseSaaS.Application/UseCases/Empresas/AlterarAmbienteEmpresaRequestValidator.cs	1970-01-01 00:00:00.000000000 +0000
+++ b/src/NfseSaaS.Application/UseCases/Empresas/AlterarAmbienteEmpresaRequestValidator.cs	2026-09-10 13:43:14.362436465 +0000
@@ -0,0 +1,11 @@
+using FluentValidation;
+
+namespace NfseSaaS.Application.UseCases.Empresas;
+
+public sealed class AlterarAmbienteEmpresaRequestValidator : AbstractValidator<AlterarAmbienteEmpresaRequest>
+{
+    public AlterarAmbienteEmpresaRequestValidator()
+    {
+        RuleFor(x => x.TipoAmbiente).IsInEnum();
+    }
+}
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Application/UseCases/Empresas/IAlterarAmbienteEmpresaUseCase.cs NfseSaaS-master/src/NfseSaaS.Application/UseCases/Empresas/IAlterarAmbienteEmpresaUseCase.cs
--- a/src/NfseSaaS.Application/UseCases/Empresas/IAlterarAmbienteEmpresaUseCase.cs	1970-01-01 00:00:00.000000000 +0000
+++ b/src/NfseSaaS.Application/UseCases/Empresas/IAlterarAmbienteEmpresaUseCase.cs	2026-09-10 13:29:17.373490919 +0000
@@ -0,0 +1,8 @@
+using NfseSaaS.Domain.Enums;
+
+namespace NfseSaaS.Application.UseCases.Empresas;
+
+public interface IAlterarAmbienteEmpresaUseCase
+{
+    Task ExecutarAsync(Guid id, TipoAmbiente novoAmbiente, CancellationToken cancellationToken);
+}
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Application/UseCases/Empresas/IPromoverEmpresaParaProducaoUseCase.cs NfseSaaS-master/src/NfseSaaS.Application/UseCases/Empresas/IPromoverEmpresaParaProducaoUseCase.cs
--- a/src/NfseSaaS.Application/UseCases/Empresas/IPromoverEmpresaParaProducaoUseCase.cs	2026-09-10 13:43:36.164629415 +0000
+++ b/src/NfseSaaS.Application/UseCases/Empresas/IPromoverEmpresaParaProducaoUseCase.cs	1970-01-01 00:00:00.000000000 +0000
@@ -1,6 +0,0 @@
-namespace NfseSaaS.Application.UseCases.Empresas;
-
-public interface IPromoverEmpresaParaProducaoUseCase
-{
-    Task ExecutarAsync(Guid id, CancellationToken cancellationToken);
-}
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Application/UseCases/Nfse/NfseResponse.cs NfseSaaS-master/src/NfseSaaS.Application/UseCases/Nfse/NfseResponse.cs
--- a/src/NfseSaaS.Application/UseCases/Nfse/NfseResponse.cs	2026-09-10 13:43:36.087361296 +0000
+++ b/src/NfseSaaS.Application/UseCases/Nfse/NfseResponse.cs	2026-09-10 13:32:15.179910584 +0000
@@ -22,6 +22,10 @@
     decimal? ValorLiquido,
     string DescricaoServico,
     NfseStatus Status,
+    // Ambiente em que ESTA nota foi emitida (congelado, ver
+    // Nfse.TipoAmbiente) — não confundir com o ambiente atual da
+    // Empresa, que pode já ter mudado.
+    TipoAmbiente TipoAmbiente,
     string? CodigoErro,
     string? MensagemErro,
     DateTimeOffset CreatedAt,
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Domain/Entities/Empresa.cs NfseSaaS-master/src/NfseSaaS.Domain/Entities/Empresa.cs
--- a/src/NfseSaaS.Domain/Entities/Empresa.cs	2026-09-10 13:43:36.164735627 +0000
+++ b/src/NfseSaaS.Domain/Entities/Empresa.cs	2026-09-10 13:42:54.261785229 +0000
@@ -70,9 +70,9 @@
     /// <summary>
     /// Em que ambiente do SEFIN Nacional as notas desta Empresa são
     /// emitidas (vira o tpAmb da DPS — ver DpsBuilder). Toda Empresa
-    /// nasce em Homologacao; a promoção para Producao é deliberada e
-    /// auditada (ver IPromoverEmpresaParaProducaoUseCase), nunca o
-    /// valor default.
+    /// nasce em Homologacao; a troca de ambiente é deliberada e
+    /// auditada nos dois sentidos (ver IAlterarAmbienteEmpresaUseCase),
+    /// nunca o valor default.
     /// </summary>
     public TipoAmbiente TipoAmbiente { get; set; } = TipoAmbiente.Homologacao;
 
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Domain/Entities/Nfse.cs NfseSaaS-master/src/NfseSaaS.Domain/Entities/Nfse.cs
--- a/src/NfseSaaS.Domain/Entities/Nfse.cs	2026-09-10 13:43:36.096965796 +0000
+++ b/src/NfseSaaS.Domain/Entities/Nfse.cs	2026-09-10 13:29:00.665960747 +0000
@@ -117,4 +117,16 @@
     public string? CodigoErro { get; set; }
 
     public string? MensagemErro { get; set; }
+
+    /// <summary>
+    /// Ambiente (Homologação/Produção) da Empresa NO MOMENTO em que esta
+    /// nota foi emitida — congelado aqui pelo mesmo motivo que
+    /// CodigoTributacaoNacional/TribIssqn etc. são snapshot: se a Empresa
+    /// mudar de ambiente depois, o histórico fiscal desta nota não pode
+    /// mudar retroativamente. Também é o que decide, daqui pra frente,
+    /// qual URL da SEFIN usar em qualquer operação futura sobre ESTA
+    /// nota (consultar, cancelar, baixar DANFSe) — nunca o ambiente
+    /// atual da Empresa.
+    /// </summary>
+    public TipoAmbiente TipoAmbiente { get; set; }
 }
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Domain/Enums/TipoAmbiente.cs NfseSaaS-master/src/NfseSaaS.Domain/Enums/TipoAmbiente.cs
--- a/src/NfseSaaS.Domain/Enums/TipoAmbiente.cs	2026-09-10 13:43:36.164844453 +0000
+++ b/src/NfseSaaS.Domain/Enums/TipoAmbiente.cs	2026-09-10 13:42:59.377929474 +0000
@@ -6,12 +6,12 @@
 /// (1 = Produção, 2 = Homologação) — sem tradução em runtime entre este
 /// enum e o XML.
 ///
-/// IMPORTANTE: isso é independente da URL do SEFIN configurada no
-/// appsettings (NfseNacionalOptions.BaseUrl) — essa URL é única e igual
-/// para todas as Empresas do sistema. O SEFIN Nacional aceita tpAmb=2
-/// (nota de teste, sem efeito fiscal) no MESMO endpoint usado pra
-/// tpAmb=1 — por isso uma Empresa nova pode ser testada em Homologação
-/// sem nenhuma mudança de configuração global.
+/// IMPORTANTE: isso é independente de qual URL do SEFIN é usada
+/// (NfseNacionalOptions.BaseUrlHomologacao/BaseUrlProducao) — são dois
+/// endpoints reais e diferentes, e a escolha de qual chamar em cada
+/// requisição é resolvida a partir DESTE enum (via
+/// TipoAmbienteExtensions.ParaTpAmb, em Infrastructure), nunca de uma
+/// configuração global fixa por deploy.
 /// </summary>
 public enum TipoAmbiente
 {
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Infrastructure/DependencyInjection.cs NfseSaaS-master/src/NfseSaaS.Infrastructure/DependencyInjection.cs
--- a/src/NfseSaaS.Infrastructure/DependencyInjection.cs	2026-09-10 13:43:36.164925413 +0000
+++ b/src/NfseSaaS.Infrastructure/DependencyInjection.cs	2026-09-10 13:29:46.000081133 +0000
@@ -116,7 +116,7 @@
         services.AddScoped<IDesativarEmpresaUseCase, DesativarEmpresaUseCase>();
         services.AddScoped<IReativarEmpresaUseCase, ReativarEmpresaUseCase>();
         services.AddScoped<IExcluirEmpresaUseCase, ExcluirEmpresaUseCase>();
-        services.AddScoped<IPromoverEmpresaParaProducaoUseCase, PromoverEmpresaParaProducaoUseCase>();
+        services.AddScoped<IAlterarAmbienteEmpresaUseCase, AlterarAmbienteEmpresaUseCase>();
 
         services.AddScoped<ICadastrarClienteUseCase, CadastrarClienteUseCase>();
         services.AddScoped<IListarClientesUseCase, ListarClientesUseCase>();
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Infrastructure/Persistence/Configurations/NfseConfiguration.cs NfseSaaS-master/src/NfseSaaS.Infrastructure/Persistence/Configurations/NfseConfiguration.cs
--- a/src/NfseSaaS.Infrastructure/Persistence/Configurations/NfseConfiguration.cs	2026-09-10 13:43:36.124503110 +0000
+++ b/src/NfseSaaS.Infrastructure/Persistence/Configurations/NfseConfiguration.cs	2026-09-10 13:29:05.793107081 +0000
@@ -76,6 +76,14 @@
         builder.Property(n => n.MensagemErro)
             .HasMaxLength(1000);
 
+        // Mesmo formato (int) usado em EmpresaConfiguration.TipoAmbiente —
+        // sem default aqui de propósito: TipoAmbiente de uma Nfse é
+        // SEMPRE definido explicitamente na criação (EmitirNfseUseCase /
+        // SincronizarNotasDaSefinUseCase), nunca deve "cair" num valor
+        // implícito.
+        builder.Property(n => n.TipoAmbiente)
+            .HasConversion<int>();
+
         // Não pode existir duas DPS com o mesmo número+série para a mesma empresa/tenant.
         builder.HasIndex(n => new { n.TenantId, n.EmpresaId, n.NumeroDps, n.SerieDps })
             .IsUnique();
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Infrastructure/TipoAmbienteExtensions.cs NfseSaaS-master/src/NfseSaaS.Infrastructure/TipoAmbienteExtensions.cs
--- a/src/NfseSaaS.Infrastructure/TipoAmbienteExtensions.cs	1970-01-01 00:00:00.000000000 +0000
+++ b/src/NfseSaaS.Infrastructure/TipoAmbienteExtensions.cs	2026-09-10 13:29:11.549490573 +0000
@@ -0,0 +1,17 @@
+using NfseSaaS.Domain.Enums;
+
+namespace NfseSaaS.Infrastructure;
+
+/// <summary>
+/// Ponto único de tradução entre TipoAmbiente (Domain) e o tpAmb (string
+/// "1"/"2") que o módulo Nacional espera — Nacional não conhece o
+/// Domain, então essa tradução só pode acontecer aqui (Infrastructure),
+/// nunca dentro de NfseSaaS.Nacional. Usado por todo use case que chama
+/// INfseApiClient/IAdnDistribuicaoClient ou monta um DpsRequest/
+/// EventoCancelamentoRequest.
+/// </summary>
+public static class TipoAmbienteExtensions
+{
+    public static string ParaTpAmb(this TipoAmbiente ambiente) =>
+        ambiente == TipoAmbiente.Producao ? "1" : "2";
+}
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Infrastructure/UseCases/AlterarAmbienteEmpresaUseCase.cs NfseSaaS-master/src/NfseSaaS.Infrastructure/UseCases/AlterarAmbienteEmpresaUseCase.cs
--- a/src/NfseSaaS.Infrastructure/UseCases/AlterarAmbienteEmpresaUseCase.cs	1970-01-01 00:00:00.000000000 +0000
+++ b/src/NfseSaaS.Infrastructure/UseCases/AlterarAmbienteEmpresaUseCase.cs	2026-09-10 13:29:43.305276461 +0000
@@ -0,0 +1,59 @@
+using Microsoft.EntityFrameworkCore;
+using NfseSaaS.Application.Abstractions;
+using NfseSaaS.Application.Exceptions;
+using NfseSaaS.Application.UseCases.Empresas;
+using NfseSaaS.Domain.Enums;
+using NfseSaaS.Infrastructure.Persistence;
+
+namespace NfseSaaS.Infrastructure.UseCases;
+
+/// <summary>
+/// Troca o ambiente (Homologação ⇄ Produção) de uma Empresa — nos DOIS
+/// sentidos (revisão da versão anterior, que só permitia promover pra
+/// Produção sem volta). Continua restrito a Administrador e auditado, nos
+/// dois sentidos: mudar PARA Produção decide se a próxima nota tem
+/// efeito fiscal real; voltar PARA Homologação é igualmente uma decisão
+/// deliberada (deixa de emitir notas reais), então merece o mesmo
+/// rastro.
+///
+/// Idempotente: se o ambiente pedido já é o atual, não faz nada e não
+/// gera entrada de auditoria.
+///
+/// NÃO reprocessa nem tenta "corrigir" notas já emitidas — cada Nfse
+/// já existente mantém o TipoAmbiente que tinha no momento em que foi
+/// emitida (ver comentário em Nfse.TipoAmbiente). Trocar o ambiente da
+/// Empresa só afeta o que ela emite/lista DAQUI PRA FRENTE.
+/// </summary>
+public sealed class AlterarAmbienteEmpresaUseCase : IAlterarAmbienteEmpresaUseCase
+{
+    private readonly AppDbContext _db;
+    private readonly IAuditLogWriter _auditLogWriter;
+
+    public AlterarAmbienteEmpresaUseCase(AppDbContext db, IAuditLogWriter auditLogWriter)
+    {
+        _db = db;
+        _auditLogWriter = auditLogWriter;
+    }
+
+    public async Task ExecutarAsync(Guid id, TipoAmbiente novoAmbiente, CancellationToken cancellationToken)
+    {
+        var empresa = await _db.Empresas.FirstOrDefaultAsync(e => e.Id == id, cancellationToken);
+
+        if (empresa is null)
+            throw new RecursoNaoEncontradoException($"Empresa {id} não encontrada.");
+
+        if (empresa.TipoAmbiente == novoAmbiente)
+            return;
+
+        var ambienteAnterior = empresa.TipoAmbiente;
+        empresa.TipoAmbiente = novoAmbiente;
+
+        _auditLogWriter.Registrar(
+            "AlterarAmbienteEmpresa",
+            "Empresa",
+            empresa.Id,
+            new { empresa.RazaoSocial, empresa.Cnpj, AmbienteAnterior = ambienteAnterior.ToString(), AmbienteNovo = novoAmbiente.ToString() });
+
+        await _db.SaveChangesAsync(cancellationToken);
+    }
+}
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Infrastructure/UseCases/CancelarNfseUseCase.cs NfseSaaS-master/src/NfseSaaS.Infrastructure/UseCases/CancelarNfseUseCase.cs
--- a/src/NfseSaaS.Infrastructure/UseCases/CancelarNfseUseCase.cs	2026-09-10 13:43:36.165505663 +0000
+++ b/src/NfseSaaS.Infrastructure/UseCases/CancelarNfseUseCase.cs	2026-09-10 13:31:22.505814208 +0000
@@ -57,7 +57,11 @@
             CnpjAutor: empresa.Cnpj,
             CodigoMotivo: request.CodigoMotivo,
             Motivo: request.Motivo,
-            TpAmb: empresa.TipoAmbiente == TipoAmbiente.Producao ? "1" : "2");
+            // Ambiente da NOTA, não da Empresa hoje — se a Empresa foi
+            // promovida/voltou de ambiente depois desta nota ter sido
+            // emitida, o cancelamento ainda precisa ir pro endpoint onde
+            // a nota realmente existe. Ver comentário em Nfse.TipoAmbiente.
+            TpAmb: nfse.TipoAmbiente.ParaTpAmb());
 
         var resposta = await _nfseNacionalService.CancelarAsync(eventoRequest, empresa.Id, cancellationToken);
 
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Infrastructure/UseCases/EmitirNfseUseCase.cs NfseSaaS-master/src/NfseSaaS.Infrastructure/UseCases/EmitirNfseUseCase.cs
--- a/src/NfseSaaS.Infrastructure/UseCases/EmitirNfseUseCase.cs	2026-09-10 13:43:36.165769905 +0000
+++ b/src/NfseSaaS.Infrastructure/UseCases/EmitirNfseUseCase.cs	2026-09-10 13:31:16.008205165 +0000
@@ -142,7 +142,8 @@
             PercentualTotalTributosSimplesNacional = empresa.PercentualTotalTributosSimplesNacional,
             IdempotencyKey = request.IdempotencyKey,
             SnapshotFiscalJson = JsonSerializer.Serialize(snapshotFiscal),
-            Status = NfseStatus.Processando
+            Status = NfseStatus.Processando,
+            TipoAmbiente = empresa.TipoAmbiente
         };
 
         _db.NotasFiscais.Add(nfse);
@@ -186,8 +187,10 @@
             // Tradução Domain → Nacional acontece só aqui, no único lugar
             // que conhece os dois lados (ver comentário em DpsRequest.TpAmb
             // sobre por que o módulo Nacional não pode fazer essa tradução
-            // sozinho).
-            TpAmb: empresa.TipoAmbiente == TipoAmbiente.Producao ? "1" : "2");
+            // sozinho). Lê de nfse.TipoAmbiente (já congelado acima), não
+            // de empresa.TipoAmbiente de novo — mesma fonte que vai pro
+            // banco, sem chance de os dois divergirem.
+            TpAmb: nfse.TipoAmbiente.ParaTpAmb());
 
         try
         {
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Infrastructure/UseCases/ListarNfseUseCase.cs NfseSaaS-master/src/NfseSaaS.Infrastructure/UseCases/ListarNfseUseCase.cs
--- a/src/NfseSaaS.Infrastructure/UseCases/ListarNfseUseCase.cs	2026-09-10 13:43:36.132609592 +0000
+++ b/src/NfseSaaS.Infrastructure/UseCases/ListarNfseUseCase.cs	2026-09-10 13:32:11.955566457 +0000
@@ -1,6 +1,7 @@
 using Microsoft.EntityFrameworkCore;
 using NfseSaaS.Application.Common;
 using NfseSaaS.Application.UseCases.Nfse;
+using NfseSaaS.Domain.Enums;
 using NfseSaaS.Infrastructure.Persistence;
 
 namespace NfseSaaS.Infrastructure.UseCases;
@@ -21,8 +22,25 @@
         var query = _db.NotasFiscais.AsNoTracking().AsQueryable();
 
         if (request.EmpresaId is not null)
+        {
             query = query.Where(n => n.EmpresaId == request.EmpresaId.Value);
 
+            // A lista só mostra notas do ambiente em que a Empresa está
+            // AGORA (Homologação ou Produção) — trocar o ambiente da
+            // Empresa muda o que aparece aqui, sem precisar de nenhum
+            // filtro adicional na tela. Cada Nfse guarda o ambiente em
+            // que nasceu (ver Nfse.TipoAmbiente), então essa consulta
+            // busca o TipoAmbiente atual da Empresa só pra filtrar — não
+            // altera nem lê nada das notas em si além do que já viria.
+            var tipoAmbienteEmpresa = await _db.Empresas.AsNoTracking()
+                .Where(e => e.Id == request.EmpresaId.Value)
+                .Select(e => (TipoAmbiente?)e.TipoAmbiente)
+                .FirstOrDefaultAsync(cancellationToken);
+
+            if (tipoAmbienteEmpresa is not null)
+                query = query.Where(n => n.TipoAmbiente == tipoAmbienteEmpresa.Value);
+        }
+
         if (request.Status is not null)
             query = query.Where(n => n.Status == request.Status.Value);
 
@@ -35,7 +53,7 @@
             .Select(n => new NfseResponse(
                 n.Id, n.EmpresaId, n.ClienteId, n.ContratoId, n.NumeroDps, n.SerieDps, n.NumeroNfse, n.ChaveAcesso,
                 n.DataCompetencia, n.DataEmissao, n.ValorServico, n.ValorLiquido, n.DescricaoServico, n.Status,
-                n.CodigoErro, n.MensagemErro, n.CreatedAt, n.UpdatedAt))
+                n.TipoAmbiente, n.CodigoErro, n.MensagemErro, n.CreatedAt, n.UpdatedAt))
             .ToListAsync(cancellationToken);
 
         return new PagedResult<NfseResponse>
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Infrastructure/UseCases/ObterDanfsePdfUseCase.cs NfseSaaS-master/src/NfseSaaS.Infrastructure/UseCases/ObterDanfsePdfUseCase.cs
--- a/src/NfseSaaS.Infrastructure/UseCases/ObterDanfsePdfUseCase.cs	2026-09-10 13:43:36.133639795 +0000
+++ b/src/NfseSaaS.Infrastructure/UseCases/ObterDanfsePdfUseCase.cs	2026-09-10 13:31:34.988103261 +0000
@@ -41,7 +41,7 @@
 
         try
         {
-            var (statusCode, bytesRemoto, contentType) = await _apiClient.ObterDanfsePdfAsync(nfse.EmpresaId, nfse.ChaveAcesso, cancellationToken);
+            var (statusCode, bytesRemoto, contentType) = await _apiClient.ObterDanfsePdfAsync(nfse.EmpresaId, nfse.TipoAmbiente.ParaTpAmb(), nfse.ChaveAcesso, cancellationToken);
 
             if (statusCode is >= 200 and < 300 && bytesRemoto.Length > 0)
             {
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Infrastructure/UseCases/ObterNfsePorIdUseCase.cs NfseSaaS-master/src/NfseSaaS.Infrastructure/UseCases/ObterNfsePorIdUseCase.cs
--- a/src/NfseSaaS.Infrastructure/UseCases/ObterNfsePorIdUseCase.cs	2026-09-10 13:43:36.134233519 +0000
+++ b/src/NfseSaaS.Infrastructure/UseCases/ObterNfsePorIdUseCase.cs	2026-09-10 13:32:24.656738254 +0000
@@ -24,6 +24,6 @@
         return new NfseResponse(
             nfse.Id, nfse.EmpresaId, nfse.ClienteId, nfse.ContratoId, nfse.NumeroDps, nfse.SerieDps, nfse.NumeroNfse, nfse.ChaveAcesso,
             nfse.DataCompetencia, nfse.DataEmissao, nfse.ValorServico, nfse.ValorLiquido, nfse.DescricaoServico, nfse.Status,
-            nfse.CodigoErro, nfse.MensagemErro, nfse.CreatedAt, nfse.UpdatedAt);
+            nfse.TipoAmbiente, nfse.CodigoErro, nfse.MensagemErro, nfse.CreatedAt, nfse.UpdatedAt);
     }
 }
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Infrastructure/UseCases/PromoverEmpresaParaProducaoUseCase.cs NfseSaaS-master/src/NfseSaaS.Infrastructure/UseCases/PromoverEmpresaParaProducaoUseCase.cs
--- a/src/NfseSaaS.Infrastructure/UseCases/PromoverEmpresaParaProducaoUseCase.cs	2026-09-10 13:43:36.166003048 +0000
+++ b/src/NfseSaaS.Infrastructure/UseCases/PromoverEmpresaParaProducaoUseCase.cs	1970-01-01 00:00:00.000000000 +0000
@@ -1,51 +0,0 @@
-using Microsoft.EntityFrameworkCore;
-using NfseSaaS.Application.Abstractions;
-using NfseSaaS.Application.Exceptions;
-using NfseSaaS.Application.UseCases.Empresas;
-using NfseSaaS.Domain.Enums;
-using NfseSaaS.Infrastructure.Persistence;
-
-namespace NfseSaaS.Infrastructure.UseCases;
-
-/// <summary>
-/// Promove uma Empresa de Homologação para Produção — ação deliberada e
-/// UNIDIRECIONAL: não existe "rebaixar de volta pra Homologação" por
-/// aqui. Se isso um dia for realmente necessário (empresa promovida por
-/// engano, por exemplo), é uma decisão grande o suficiente pra passar
-/// por uma alteração direta no banco com registro manual do motivo, não
-/// por um botão na tela — o mesmo raciocínio de "sem bypass, sem
-/// gambiarra" aplicado a um campo que decide se a próxima nota emitida
-/// tem efeito fiscal real.
-///
-/// Idempotente: se a Empresa já está em Produção, não faz nada e não
-/// gera entrada de auditoria — nada mudou de fato (mesmo padrão de
-/// DesativarEmpresaUseCase).
-/// </summary>
-public sealed class PromoverEmpresaParaProducaoUseCase : IPromoverEmpresaParaProducaoUseCase
-{
-    private readonly AppDbContext _db;
-    private readonly IAuditLogWriter _auditLogWriter;
-
-    public PromoverEmpresaParaProducaoUseCase(AppDbContext db, IAuditLogWriter auditLogWriter)
-    {
-        _db = db;
-        _auditLogWriter = auditLogWriter;
-    }
-
-    public async Task ExecutarAsync(Guid id, CancellationToken cancellationToken)
-    {
-        var empresa = await _db.Empresas.FirstOrDefaultAsync(e => e.Id == id, cancellationToken);
-
-        if (empresa is null)
-            throw new RecursoNaoEncontradoException($"Empresa {id} não encontrada.");
-
-        if (empresa.TipoAmbiente == TipoAmbiente.Producao)
-            return;
-
-        empresa.TipoAmbiente = TipoAmbiente.Producao;
-
-        _auditLogWriter.Registrar("PromoverEmpresaParaProducao", "Empresa", empresa.Id, new { empresa.RazaoSocial, empresa.Cnpj });
-
-        await _db.SaveChangesAsync(cancellationToken);
-    }
-}
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Infrastructure/UseCases/SincronizarNotasDaSefinUseCase.cs NfseSaaS-master/src/NfseSaaS.Infrastructure/UseCases/SincronizarNotasDaSefinUseCase.cs
--- a/src/NfseSaaS.Infrastructure/UseCases/SincronizarNotasDaSefinUseCase.cs	2026-09-10 13:43:36.135426702 +0000
+++ b/src/NfseSaaS.Infrastructure/UseCases/SincronizarNotasDaSefinUseCase.cs	2026-09-10 13:31:53.979087957 +0000
@@ -50,7 +50,7 @@
             ?? throw new RecursoNaoEncontradoException($"Empresa {empresaId} não encontrada.");
 
         var ultimoNsu = empresa.UltimoNsuDistribuicao ?? 0;
-        var lote = await _adnClient.ConsultarPorNsuAsync(empresaId, ultimoNsu, cancellationToken);
+        var lote = await _adnClient.ConsultarPorNsuAsync(empresaId, empresa.TipoAmbiente.ParaTpAmb(), ultimoNsu, cancellationToken);
 
         int importadas = 0, jaExistentes = 0, ignoradasPorConflito = 0, clientesCriados = 0;
         var maiorNsu = ultimoNsu;
@@ -159,7 +159,16 @@
                 ValorLiquido = dados.ValorLiquido(),
                 XmlNfse = xml,
                 SnapshotFiscalJson = JsonSerializer.Serialize(snapshot),
-                Status = status
+                Status = status,
+                // Simplificação conhecida: usa o ambiente ATUAL da
+                // Empresa, não o tpAmb original gravado no XML importado
+                // (NfseXmlImportDados ainda não expõe esse campo). Numa
+                // sincronização normal (mesmo dia/mesmo ambiente) dá no
+                // mesmo; o caso raro que fica errado é importar, via ADN,
+                // uma nota antiga de um ambiente que a Empresa já não
+                // está mais — aceitável por ora, mas documentado aqui pra
+                // não virar surpresa depois.
+                TipoAmbiente = empresa.TipoAmbiente
             };
 
             _db.NotasFiscais.Add(nfse);
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Infrastructure/UseCases/TestarConexaoCertificadoUseCase.cs NfseSaaS-master/src/NfseSaaS.Infrastructure/UseCases/TestarConexaoCertificadoUseCase.cs
--- a/src/NfseSaaS.Infrastructure/UseCases/TestarConexaoCertificadoUseCase.cs	2026-09-10 13:43:36.135950525 +0000
+++ b/src/NfseSaaS.Infrastructure/UseCases/TestarConexaoCertificadoUseCase.cs	2026-09-10 13:31:42.657253561 +0000
@@ -35,14 +35,16 @@
     public async Task<TesteConexaoResponse> ExecutarAsync(Guid empresaId, CancellationToken cancellationToken)
     {
         // Ver comentário completo em EnviarCertificadoUseCase — mesma
-        // checagem obrigatória de tenant via _db.Empresas.
-        var empresaExiste = await _db.Empresas.AnyAsync(e => e.Id == empresaId, cancellationToken);
-        if (!empresaExiste)
+        // checagem obrigatória de tenant via _db.Empresas. Carrega a
+        // entidade inteira (não só AnyAsync) porque agora também
+        // precisamos do TipoAmbiente pra saber qual URL da SEFIN chamar.
+        var empresa = await _db.Empresas.FirstOrDefaultAsync(e => e.Id == empresaId, cancellationToken);
+        if (empresa is null)
             throw new RecursoNaoEncontradoException($"Empresa {empresaId} não encontrada.");
 
         try
         {
-            var (statusCode, _) = await _apiClient.ConsultarPorChaveAsync(empresaId, ChaveDeTeste, cancellationToken);
+            var (statusCode, _) = await _apiClient.ConsultarPorChaveAsync(empresaId, empresa.TipoAmbiente.ParaTpAmb(), ChaveDeTeste, cancellationToken);
 
             return new TesteConexaoResponse(
                 Sucesso: true,
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Nacional/Clients/AdnDistribuicaoClient.cs NfseSaaS-master/src/NfseSaaS.Nacional/Clients/AdnDistribuicaoClient.cs
--- a/src/NfseSaaS.Nacional/Clients/AdnDistribuicaoClient.cs	2026-09-10 13:43:36.137804727 +0000
+++ b/src/NfseSaaS.Nacional/Clients/AdnDistribuicaoClient.cs	2026-09-10 13:30:59.592963114 +0000
@@ -13,8 +13,8 @@
 /// só olha o NOME do client, não a URL chamada nele. Como o certificado é
 /// o mesmo (por Empresa) tanto pra emissão quanto pra distribuição de
 /// DF-e, não há necessidade de uma convenção de nome de client separada —
-/// só aponta pra uma URL absoluta diferente (host do ADN) na mesma
-/// chamada GetAsync.
+/// só aponta pra uma URL absoluta diferente (host do ADN, resolvida por
+/// tpAmb via AdnOptions.ObterBaseUrl) na mesma chamada GetAsync.
 /// </summary>
 public sealed class AdnDistribuicaoClient : IAdnDistribuicaoClient
 {
@@ -27,16 +27,21 @@
         _options = options.Value;
     }
 
-    public async Task<DfeLoteResponse> ConsultarPorNsuAsync(Guid empresaId, long ultimoNsuProcessado, CancellationToken cancellationToken)
+    public async Task<DfeLoteResponse> ConsultarPorNsuAsync(Guid empresaId, string tpAmb, long ultimoNsuProcessado, CancellationToken cancellationToken)
     {
-        if (string.IsNullOrWhiteSpace(_options.BaseUrl))
-            throw new NfseApiException("Adn:BaseUrl não configurada.");
+        var baseUrlConfigurada = _options.ObterBaseUrl(tpAmb);
+
+        if (string.IsNullOrWhiteSpace(baseUrlConfigurada))
+        {
+            var nomeCampo = tpAmb == "1" ? "BaseUrlProducao" : "BaseUrlHomologacao";
+            throw new NfseApiException($"Adn:{nomeCampo} não configurada.");
+        }
 
         var client = _httpClientFactory.CreateClient(SefinNacionalClientNames.ParaEmpresa(empresaId));
         using var timeoutCts = CancellationTokenSource.CreateLinkedTokenSource(cancellationToken);
         timeoutCts.CancelAfter(TimeSpan.FromSeconds(_options.TimeoutSeconds));
 
-        var baseUrl = _options.BaseUrl.EndsWith('/') ? _options.BaseUrl : _options.BaseUrl + "/";
+        var baseUrl = baseUrlConfigurada.EndsWith('/') ? baseUrlConfigurada : baseUrlConfigurada + "/";
         var url = new Uri(new Uri(baseUrl), $"DFe/{ultimoNsuProcessado}");
 
         try
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Nacional/Clients/IAdnDistribuicaoClient.cs NfseSaaS-master/src/NfseSaaS.Nacional/Clients/IAdnDistribuicaoClient.cs
--- a/src/NfseSaaS.Nacional/Clients/IAdnDistribuicaoClient.cs	2026-09-10 13:43:36.138678111 +0000
+++ b/src/NfseSaaS.Nacional/Clients/IAdnDistribuicaoClient.cs	2026-09-10 13:30:59.591684694 +0000
@@ -9,5 +9,6 @@
 public interface IAdnDistribuicaoClient
 {
     /// <summary>Retorna todos os documentos disponíveis com NSU maior que o informado.</summary>
-    Task<DfeLoteResponse> ConsultarPorNsuAsync(Guid empresaId, long ultimoNsuProcessado, CancellationToken cancellationToken);
+    /// <param name="tpAmb">"1" = Produção, "2" = Homologação — decide qual das duas URLs (AdnOptions) é chamada.</param>
+    Task<DfeLoteResponse> ConsultarPorNsuAsync(Guid empresaId, string tpAmb, long ultimoNsuProcessado, CancellationToken cancellationToken);
 }
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Nacional/Clients/INfseApiClient.cs NfseSaaS-master/src/NfseSaaS.Nacional/Clients/INfseApiClient.cs
--- a/src/NfseSaaS.Nacional/Clients/INfseApiClient.cs	2026-09-10 13:43:36.138747689 +0000
+++ b/src/NfseSaaS.Nacional/Clients/INfseApiClient.cs	2026-09-10 13:30:23.271367278 +0000
@@ -4,10 +4,12 @@
 public interface INfseApiClient
 {
     /// <summary>Envia a DPS (já assinada, compactada em GZip+Base64) via POST /nfse, usando o certificado mTLS da Empresa informada.</summary>
-    Task<(int StatusCode, string Body)> EnviarDpsAsync(Guid empresaId, string dpsXmlGZipBase64, CancellationToken cancellationToken);
+    /// <param name="tpAmb">"1" = Produção, "2" = Homologação — decide qual das duas URLs (NfseNacionalOptions) é chamada.</param>
+    Task<(int StatusCode, string Body)> EnviarDpsAsync(Guid empresaId, string tpAmb, string dpsXmlGZipBase64, CancellationToken cancellationToken);
 
     /// <summary>Consulta uma NFS-e já emitida pela chave de acesso via GET /nfse/{chaveAcesso}, usando o certificado mTLS da Empresa informada.</summary>
-    Task<(int StatusCode, string Body)> ConsultarPorChaveAsync(Guid empresaId, string chaveAcesso, CancellationToken cancellationToken);
+    /// <param name="tpAmb">"1" = Produção, "2" = Homologação.</param>
+    Task<(int StatusCode, string Body)> ConsultarPorChaveAsync(Guid empresaId, string tpAmb, string chaveAcesso, CancellationToken cancellationToken);
 
     /// <summary>
     /// Baixa a representação gráfica (DANFSe) em PDF via GET /danfse/{chaveAcesso}.
@@ -18,8 +20,10 @@
     /// forma consistente, é sinal de que a geração precisa passar a ser
     /// local (fora do escopo desta implementação).
     /// </summary>
-    Task<(int StatusCode, byte[] Bytes, string? ContentType)> ObterDanfsePdfAsync(Guid empresaId, string chaveAcesso, CancellationToken cancellationToken);
+    /// <param name="tpAmb">"1" = Produção, "2" = Homologação — deve ser o ambiente em que a nota foi emitida, não o ambiente atual da Empresa.</param>
+    Task<(int StatusCode, byte[] Bytes, string? ContentType)> ObterDanfsePdfAsync(Guid empresaId, string tpAmb, string chaveAcesso, CancellationToken cancellationToken);
 
     /// <summary>Envia um evento (já assinado, compactado em GZip+Base64) via POST /nfse/{chaveAcesso}/eventos, usando o certificado mTLS da Empresa informada.</summary>
-    Task<(int StatusCode, string Body)> EnviarEventoAsync(Guid empresaId, string chaveAcesso, string eventoXmlGZipBase64, CancellationToken cancellationToken);
+    /// <param name="tpAmb">"1" = Produção, "2" = Homologação — deve ser o ambiente em que a nota foi emitida, não o ambiente atual da Empresa.</param>
+    Task<(int StatusCode, string Body)> EnviarEventoAsync(Guid empresaId, string tpAmb, string chaveAcesso, string eventoXmlGZipBase64, CancellationToken cancellationToken);
 }
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Nacional/Clients/NfseApiClient.cs NfseSaaS-master/src/NfseSaaS.Nacional/Clients/NfseApiClient.cs
--- a/src/NfseSaaS.Nacional/Clients/NfseApiClient.cs	2026-09-10 13:43:36.138960171 +0000
+++ b/src/NfseSaaS.Nacional/Clients/NfseApiClient.cs	2026-09-10 13:30:45.697496169 +0000
@@ -13,10 +13,10 @@
 /// convenção de nome de SefinNacionalClientNames — ver comentário completo
 /// da decisão de arquitetura em CertificateHttpMessageHandlerBuilderFilter.
 ///
-/// A URL base não é configurada via HttpClient.BaseAddress (que exigiria
-/// nomes de client conhecidos em tempo de registro); em vez disso, é
-/// montada a partir de NfseNacionalOptions a cada chamada, o que também
-/// permite trocar o ambiente (homologação/produção) sem recompilar.
+/// A URL base é resolvida A CADA CHAMADA a partir de NfseNacionalOptions
+/// E do tpAmb informado pelo chamador — NfseNacionalOptions.ObterBaseUrl
+/// escolhe entre BaseUrlHomologacao/BaseUrlProducao. Este client não
+/// decide sozinho qual ambiente usar; só executa o que recebeu.
 /// </summary>
 public sealed class NfseApiClient : INfseApiClient
 {
@@ -31,7 +31,7 @@
         _logger = logger;
     }
 
-    public async Task<(int StatusCode, string Body)> EnviarDpsAsync(Guid empresaId, string dpsXmlGZipBase64, CancellationToken cancellationToken)
+    public async Task<(int StatusCode, string Body)> EnviarDpsAsync(Guid empresaId, string tpAmb, string dpsXmlGZipBase64, CancellationToken cancellationToken)
     {
         var client = ObterClient(empresaId);
         using var timeoutCts = CriarTokenComTimeout(cancellationToken);
@@ -39,7 +39,7 @@
         try
         {
             var payload = new { dpsXmlGZipB64 = dpsXmlGZipBase64 };
-            using var response = await client.PostAsJsonAsync(MontarUrl("nfse"), payload, timeoutCts.Token);
+            using var response = await client.PostAsJsonAsync(MontarUrl("nfse", tpAmb), payload, timeoutCts.Token);
             var body = await response.Content.ReadAsStringAsync(cancellationToken);
             return ((int)response.StatusCode, body);
         }
@@ -53,14 +53,14 @@
         }
     }
 
-    public async Task<(int StatusCode, string Body)> ConsultarPorChaveAsync(Guid empresaId, string chaveAcesso, CancellationToken cancellationToken)
+    public async Task<(int StatusCode, string Body)> ConsultarPorChaveAsync(Guid empresaId, string tpAmb, string chaveAcesso, CancellationToken cancellationToken)
     {
         var client = ObterClient(empresaId);
         using var timeoutCts = CriarTokenComTimeout(cancellationToken);
 
         try
         {
-            using var response = await client.GetAsync(MontarUrl($"nfse/{chaveAcesso}"), timeoutCts.Token);
+            using var response = await client.GetAsync(MontarUrl($"nfse/{chaveAcesso}", tpAmb), timeoutCts.Token);
             var body = await response.Content.ReadAsStringAsync(cancellationToken);
             return ((int)response.StatusCode, body);
         }
@@ -74,7 +74,7 @@
         }
     }
 
-    public async Task<(int StatusCode, string Body)> EnviarEventoAsync(Guid empresaId, string chaveAcesso, string eventoXmlGZipBase64, CancellationToken cancellationToken)
+    public async Task<(int StatusCode, string Body)> EnviarEventoAsync(Guid empresaId, string tpAmb, string chaveAcesso, string eventoXmlGZipBase64, CancellationToken cancellationToken)
     {
         var client = ObterClient(empresaId);
         using var timeoutCts = CriarTokenComTimeout(cancellationToken);
@@ -84,7 +84,7 @@
             // Nome do campo confirmado no Swagger oficial da SEFIN Nacional
             // (schema EventosPostRequest): "pedidoRegistroEventoXmlGZipB64".
             var payload = new { pedidoRegistroEventoXmlGZipB64 = eventoXmlGZipBase64 };
-            using var response = await client.PostAsJsonAsync(MontarUrl($"nfse/{chaveAcesso}/eventos"), payload, timeoutCts.Token);
+            using var response = await client.PostAsJsonAsync(MontarUrl($"nfse/{chaveAcesso}/eventos", tpAmb), payload, timeoutCts.Token);
             var body = await response.Content.ReadAsStringAsync(cancellationToken);
 
             if (!response.IsSuccessStatusCode)
@@ -111,14 +111,14 @@
         }
     }
 
-    public async Task<(int StatusCode, byte[] Bytes, string? ContentType)> ObterDanfsePdfAsync(Guid empresaId, string chaveAcesso, CancellationToken cancellationToken)
+    public async Task<(int StatusCode, byte[] Bytes, string? ContentType)> ObterDanfsePdfAsync(Guid empresaId, string tpAmb, string chaveAcesso, CancellationToken cancellationToken)
     {
         var client = ObterClient(empresaId);
         using var timeoutCts = CriarTokenComTimeout(cancellationToken);
 
         try
         {
-            using var response = await client.GetAsync(MontarUrl($"danfse/{chaveAcesso}"), timeoutCts.Token);
+            using var response = await client.GetAsync(MontarUrl($"danfse/{chaveAcesso}", tpAmb), timeoutCts.Token);
             var bytes = await response.Content.ReadAsByteArrayAsync(cancellationToken);
             return ((int)response.StatusCode, bytes, response.Content.Headers.ContentType?.MediaType);
         }
@@ -135,12 +135,17 @@
     private HttpClient ObterClient(Guid empresaId) =>
         _httpClientFactory.CreateClient(SefinNacionalClientNames.ParaEmpresa(empresaId));
 
-    private Uri MontarUrl(string caminhoRelativo)
+    private Uri MontarUrl(string caminhoRelativo, string tpAmb)
     {
-        if (string.IsNullOrWhiteSpace(_options.BaseUrl))
-            throw new NfseApiException("NfseNacional:BaseUrl não configurada.");
+        var baseUrlConfigurada = _options.ObterBaseUrl(tpAmb);
 
-        var baseUrl = _options.BaseUrl.EndsWith('/') ? _options.BaseUrl : _options.BaseUrl + "/";
+        if (string.IsNullOrWhiteSpace(baseUrlConfigurada))
+        {
+            var nomeCampo = tpAmb == "1" ? "BaseUrlProducao" : "BaseUrlHomologacao";
+            throw new NfseApiException($"NfseNacional:{nomeCampo} não configurada.");
+        }
+
+        var baseUrl = baseUrlConfigurada.EndsWith('/') ? baseUrlConfigurada : baseUrlConfigurada + "/";
         return new Uri(new Uri(baseUrl), caminhoRelativo);
     }
 
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Nacional/Options/AdnOptions.cs NfseSaaS-master/src/NfseSaaS.Nacional/Options/AdnOptions.cs
--- a/src/NfseSaaS.Nacional/Options/AdnOptions.cs	2026-09-10 13:43:36.142522615 +0000
+++ b/src/NfseSaaS.Nacional/Options/AdnOptions.cs	2026-09-10 13:30:13.611578786 +0000
@@ -2,19 +2,27 @@
 
 /// <summary>
 /// O ADN (Ambiente de Dados Nacional) é um serviço da NFS-e Nacional
-/// DIFERENTE do SEFIN de emissão (NfseNacionalOptions) — host próprio
-/// (adn.nfse.gov.br / adn.producaorestrita.nfse.gov.br), mas usa o MESMO
-/// certificado mTLS por Empresa. Serve pra "puxar de volta" documentos
-/// fiscais emitidos por QUALQUER canal (API, portal web Emissor Nacional,
-/// outro sistema) em que o CNPJ da Empresa figure como emitente, tomador
-/// ou intermediário — mecanismo oficial de "Distribuição de DF-e por NSU".
+/// DIFERENTE do SEFIN de emissão (NfseNacionalOptions) — host próprio,
+/// mas usa o MESMO certificado mTLS por Empresa. Serve pra "puxar de
+/// volta" documentos fiscais emitidos por QUALQUER canal (API, portal
+/// web Emissor Nacional, outro sistema) em que o CNPJ da Empresa figure
+/// como emitente, tomador ou intermediário — mecanismo oficial de
+/// "Distribuição de DF-e por NSU".
+///
+/// Mesma dualidade de URL do NfseNacionalOptions: Homologação
+/// (adn.producaorestrita.nfse.gov.br) e Produção (adn.nfse.gov.br) são
+/// hosts diferentes de verdade.
 /// </summary>
 public sealed class AdnOptions
 {
     public const string SectionName = "Adn";
 
-    /// <summary>Ex.: https://adn.producaorestrita.nfse.gov.br/contribuintes (homologação) ou https://adn.nfse.gov.br/contribuintes (produção).</summary>
-    public string BaseUrl { get; set; } = string.Empty;
+    public string BaseUrlHomologacao { get; set; } = string.Empty;
+
+    public string BaseUrlProducao { get; set; } = string.Empty;
 
     public int TimeoutSeconds { get; set; } = 60;
+
+    /// <summary>Resolve a BaseUrl certa a partir do tpAmb ("1" = Produção, "2" = Homologação).</summary>
+    public string ObterBaseUrl(string tpAmb) => tpAmb == "1" ? BaseUrlProducao : BaseUrlHomologacao;
 }
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Nacional/Options/NfseNacionalOptions.cs NfseSaaS-master/src/NfseSaaS.Nacional/Options/NfseNacionalOptions.cs
--- a/src/NfseSaaS.Nacional/Options/NfseNacionalOptions.cs	2026-09-10 13:43:36.142686164 +0000
+++ b/src/NfseSaaS.Nacional/Options/NfseNacionalOptions.cs	2026-09-10 13:30:07.799600573 +0000
@@ -3,17 +3,24 @@
 /// <summary>
 /// Configuração fortemente tipada da integração com a SEFIN Nacional,
 /// vinculada via IOptions&lt;NfseNacionalOptions&gt; à seção "NfseNacional"
-/// do appsettings. A URL é configurável sem recompilar a aplicação.
+/// do appsettings.
+///
+/// DUAS URLs, não uma: Homologação e Produção são endpoints REAIS e
+/// diferentes da SEFIN Nacional (não é só uma flag tpAmb no mesmo host).
+/// Qual delas é usada em cada chamada é decidido POR CHAMADA, a partir
+/// do tpAmb resolvido pelo caller (empresa ou nota, conforme o caso) —
+/// ver ObterBaseUrl. Nunca existe uma "URL do sistema" única.
 /// </summary>
 public sealed class NfseNacionalOptions
 {
     public const string SectionName = "NfseNacional";
 
-    /// <summary>URL base da SEFIN Nacional (ex.: ambiente de produção restrita/homologação ou produção).</summary>
-    public string BaseUrl { get; set; } = string.Empty;
+    public string BaseUrlHomologacao { get; set; } = string.Empty;
 
-    /// <summary>Identificação do ambiente ("Homologacao", "ProducaoRestrita", "Producao").</summary>
-    public string Ambiente { get; set; } = "Homologacao";
+    public string BaseUrlProducao { get; set; } = string.Empty;
 
     public int TimeoutSeconds { get; set; } = 60;
+
+    /// <summary>Resolve a BaseUrl certa a partir do tpAmb ("1" = Produção, "2" = Homologação).</summary>
+    public string ObterBaseUrl(string tpAmb) => tpAmb == "1" ? BaseUrlProducao : BaseUrlHomologacao;
 }
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Nacional/Services/NfseNacionalService.cs NfseSaaS-master/src/NfseSaaS.Nacional/Services/NfseNacionalService.cs
--- a/src/NfseSaaS.Nacional/Services/NfseNacionalService.cs	2026-09-10 13:43:36.143265743 +0000
+++ b/src/NfseSaaS.Nacional/Services/NfseNacionalService.cs	2026-09-10 13:31:05.221933422 +0000
@@ -59,7 +59,7 @@
         var xmlAssinado = _signer.Assinar(xmlDps, infDpsId, certificado);
         var gzipBase64 = GZipHelper.ComprimirParaBase64(xmlAssinado);
 
-        var (statusCode, body) = await _apiClient.EnviarDpsAsync(empresaId, gzipBase64, cancellationToken);
+        var (statusCode, body) = await _apiClient.EnviarDpsAsync(empresaId, request.TpAmb, gzipBase64, cancellationToken);
 
         return NfseResponseParser.Parse(statusCode, body);
     }
@@ -72,7 +72,7 @@
         var xmlAssinado = _signer.Assinar(xmlEvento, infPedRegId, certificado);
         var gzipBase64 = GZipHelper.ComprimirParaBase64(xmlAssinado);
 
-        var (statusCode, body) = await _apiClient.EnviarEventoAsync(empresaId, request.ChaveAcesso, gzipBase64, cancellationToken);
+        var (statusCode, body) = await _apiClient.EnviarEventoAsync(empresaId, request.TpAmb, request.ChaveAcesso, gzipBase64, cancellationToken);
 
         return EventoResponseParser.Parse(statusCode, body);
     }
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Web/Controllers/Api/EmpresasController.cs NfseSaaS-master/src/NfseSaaS.Web/Controllers/Api/EmpresasController.cs
--- a/src/NfseSaaS.Web/Controllers/Api/EmpresasController.cs	2026-09-10 13:43:36.166910130 +0000
+++ b/src/NfseSaaS.Web/Controllers/Api/EmpresasController.cs	2026-09-10 13:30:01.585985426 +0000
@@ -19,7 +19,7 @@
     private readonly IDesativarEmpresaUseCase _desativarEmpresa;
     private readonly IReativarEmpresaUseCase _reativarEmpresa;
     private readonly IExcluirEmpresaUseCase _excluirEmpresa;
-    private readonly IPromoverEmpresaParaProducaoUseCase _promoverParaProducao;
+    private readonly IAlterarAmbienteEmpresaUseCase _alterarAmbiente;
     private readonly IEnviarCertificadoUseCase _enviarCertificado;
     private readonly IObterStatusCertificadoUseCase _obterStatusCertificado;
     private readonly ITestarConexaoCertificadoUseCase _testarConexaoCertificado;
@@ -33,7 +33,7 @@
         IDesativarEmpresaUseCase desativarEmpresa,
         IReativarEmpresaUseCase reativarEmpresa,
         IExcluirEmpresaUseCase excluirEmpresa,
-        IPromoverEmpresaParaProducaoUseCase promoverParaProducao,
+        IAlterarAmbienteEmpresaUseCase alterarAmbiente,
         IEnviarCertificadoUseCase enviarCertificado,
         IObterStatusCertificadoUseCase obterStatusCertificado,
         ITestarConexaoCertificadoUseCase testarConexaoCertificado,
@@ -46,7 +46,7 @@
         _desativarEmpresa = desativarEmpresa;
         _reativarEmpresa = reativarEmpresa;
         _excluirEmpresa = excluirEmpresa;
-        _promoverParaProducao = promoverParaProducao;
+        _alterarAmbiente = alterarAmbiente;
         _enviarCertificado = enviarCertificado;
         _obterStatusCertificado = obterStatusCertificado;
         _testarConexaoCertificado = testarConexaoCertificado;
@@ -116,17 +116,17 @@
     }
 
     /// <summary>
-    /// Promove a Empresa de Homologação para Produção — a partir daqui,
-    /// toda nova Nfse emitida por ela vai com tpAmb=1 (efeito fiscal
-    /// real). Ação unidirecional e auditada — não existe endpoint pra
-    /// voltar. Idempotente: chamar de novo numa Empresa já em Produção
-    /// não faz nada (204 do mesmo jeito).
+    /// Troca o ambiente (Homologação ⇄ Produção) da Empresa — nos dois
+    /// sentidos. A partir daqui, toda nova Nfse emitida por ela usa o
+    /// ambiente novo; notas já emitidas mantêm o ambiente que tinham na
+    /// hora (ver Nfse.TipoAmbiente). Idempotente: chamar de novo com o
+    /// mesmo ambiente atual não faz nada (204 do mesmo jeito).
     /// </summary>
     [Authorize(Roles = Papeis.Administrador)]
-    [HttpPost("{id:guid}/promover-producao")]
-    public async Task<IActionResult> PromoverParaProducao(Guid id, CancellationToken cancellationToken)
+    [HttpPut("{id:guid}/ambiente")]
+    public async Task<IActionResult> AlterarAmbiente(Guid id, [FromBody] AlterarAmbienteEmpresaRequest request, CancellationToken cancellationToken)
     {
-        await _promoverParaProducao.ExecutarAsync(id, cancellationToken);
+        await _alterarAmbiente.ExecutarAsync(id, request.TipoAmbiente, cancellationToken);
         return NoContent();
     }
 
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Web/Views/Clientes/Index.cshtml NfseSaaS-master/src/NfseSaaS.Web/Views/Clientes/Index.cshtml
--- a/src/NfseSaaS.Web/Views/Clientes/Index.cshtml	2026-09-10 13:43:36.150617400 +0000
+++ b/src/NfseSaaS.Web/Views/Clientes/Index.cshtml	2026-09-10 13:41:14.798712442 +0000
@@ -3,13 +3,6 @@
     var podeEditar = User.IsInRole("Administrador");
 }
 
-<div class="card mb-3">
-    <div class="card-body">
-        <label class="form-label">Empresa</label>
-        <select id="seletor-empresa" class="form-select"></select>
-    </div>
-</div>
-
 <div class="card">
     <div class="card-header d-flex justify-content-between align-items-center">
         <span>Clientes cadastrados</span>
@@ -121,6 +114,5 @@
 }
 
 @section Scripts {
-    <script src="~/js/empresa-seletor.js"></script>
     <script src="~/js/paginas/clientes.js" asp-append-version="true"></script>
 }
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Web/Views/Contratos/Index.cshtml NfseSaaS-master/src/NfseSaaS.Web/Views/Contratos/Index.cshtml
--- a/src/NfseSaaS.Web/Views/Contratos/Index.cshtml	2026-09-10 13:43:36.151028542 +0000
+++ b/src/NfseSaaS.Web/Views/Contratos/Index.cshtml	2026-09-10 13:41:14.811326257 +0000
@@ -3,13 +3,6 @@
     var podeEditar = User.IsInRole("Administrador");
 }
 
-<div class="card mb-3">
-    <div class="card-body">
-        <label class="form-label">Empresa</label>
-        <select id="seletor-empresa" class="form-select"></select>
-    </div>
-</div>
-
 <div class="card">
     <div class="card-header d-flex justify-content-between align-items-center">
         <span>Contratos cadastrados</span>
@@ -171,6 +164,5 @@
 }
 
 @section Scripts {
-    <script src="~/js/empresa-seletor.js"></script>
     <script src="~/js/paginas/contratos.js" asp-append-version="true"></script>
 }
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Web/Views/Nfse/Index.cshtml NfseSaaS-master/src/NfseSaaS.Web/Views/Nfse/Index.cshtml
--- a/src/NfseSaaS.Web/Views/Nfse/Index.cshtml	2026-09-10 13:43:36.152699842 +0000
+++ b/src/NfseSaaS.Web/Views/Nfse/Index.cshtml	2026-09-10 13:41:23.414711207 +0000
@@ -4,13 +4,6 @@
     var podeCancelar = User.IsInRole("Administrador");
 }
 
-<div class="card mb-3">
-    <div class="card-body">
-        <label class="form-label">Empresa</label>
-        <select id="seletor-empresa" class="form-select"></select>
-    </div>
-</div>
-
 <div class="card">
     <div class="card-header d-flex justify-content-between align-items-center">
         <span>Notas fiscais emitidas</span>
@@ -164,6 +157,5 @@
 }
 
 @section Scripts {
-    <script src="~/js/empresa-seletor.js"></script>
     <script src="~/js/paginas/nfse.js" asp-append-version="true"></script>
 }
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Web/Views/Servicos/Index.cshtml NfseSaaS-master/src/NfseSaaS.Web/Views/Servicos/Index.cshtml
--- a/src/NfseSaaS.Web/Views/Servicos/Index.cshtml	2026-09-10 13:43:36.152848750 +0000
+++ b/src/NfseSaaS.Web/Views/Servicos/Index.cshtml	2026-09-10 13:41:14.825218055 +0000
@@ -3,13 +3,6 @@
     var podeEditar = User.IsInRole("Administrador");
 }
 
-<div class="card mb-3">
-    <div class="card-body">
-        <label class="form-label">Empresa</label>
-        <select id="seletor-empresa" class="form-select"></select>
-    </div>
-</div>
-
 <div class="card">
     <div class="card-header d-flex justify-content-between align-items-center">
         <span>Serviços cadastrados</span>
@@ -84,6 +77,5 @@
 }
 
 @section Scripts {
-    <script src="~/js/empresa-seletor.js"></script>
     <script src="~/js/paginas/servicos.js" asp-append-version="true"></script>
 }
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Web/Views/Shared/_Layout.cshtml NfseSaaS-master/src/NfseSaaS.Web/Views/Shared/_Layout.cshtml
--- a/src/NfseSaaS.Web/Views/Shared/_Layout.cshtml	2026-09-10 13:43:36.167299805 +0000
+++ b/src/NfseSaaS.Web/Views/Shared/_Layout.cshtml	2026-09-10 13:43:26.617248748 +0000
@@ -1,4 +1,4 @@
-@inject Microsoft.Extensions.Options.IOptions<NfseSaaS.Nacional.Options.NfseNacionalOptions> NfseNacionalOptionsAccessor
+@inject NfseSaaS.Application.UseCases.Empresas.IObterEmpresaPorIdUseCase ObterEmpresaPorIdUseCase
 @{
     var papelAdministrador = User.IsInRole("Administrador");
     var caminhoAtual = Context.Request.Path.Value?.TrimEnd('/') ?? "";
@@ -7,17 +7,33 @@
     var iniciais = string.Concat(nomeUsuario.Split(new[] { '@', '.', ' ' }, StringSplitOptions.RemoveEmptyEntries)
         .Take(2).Select(p => char.ToUpperInvariant(p[0])));
 
-    // Badge global: contra qual endpoint da SEFIN ESTE DEPLOY está
-    // configurado (appsettings NfseNacional:Ambiente) — não confundir
-    // com o ambiente de cada Empresa (esse fica na tela de Empresas).
-    // "ProducaoRestrita" é ambiente de teste do governo, mas o nome não
-    // deixa isso óbvio pra quem só olha o badge — por isso o rótulo e a
-    // cor tratam ela como Homologação (só "Producao" puro é Produção de
-    // verdade).
-    var ambienteSistema = NfseNacionalOptionsAccessor.Value.Ambiente;
-    var sistemaEmProducao = ambienteSistema == "Producao";
-    var rotuloAmbienteSistema = sistemaEmProducao ? "Produção" : "Homologação";
-    var corAmbienteSistema = sistemaEmProducao ? "danger" : "warning";
+    // Badge de ambiente: vem SEMPRE da Empresa selecionada (cookie
+    // EmpresaAtualId, escrito por empresa-atual.js), nunca de config —
+    // não existe mais "ambiente do sistema". Resolvido aqui, no
+    // servidor, só pra o badge já nascer certo no primeiro render (sem
+    // esperar o JS popular o <select> do topo); o <select> em si
+    // continua sendo montado no cliente (empresa-atual.js), então o
+    // nome/CNPJ das opções não precisa ser buscado de novo aqui.
+    string? rotuloAmbienteEmpresa = null;
+    string corAmbienteEmpresa = "secondary";
+
+    if (Guid.TryParse(Context.Request.Cookies["EmpresaAtualId"], out var empresaAtualId))
+    {
+        try
+        {
+            var empresaAtual = await ObterEmpresaPorIdUseCase.ExecutarAsync(empresaAtualId, Context.RequestAborted);
+            var emProducao = empresaAtual.TipoAmbiente == NfseSaaS.Domain.Enums.TipoAmbiente.Producao;
+            rotuloAmbienteEmpresa = emProducao ? "Produção" : "Homologação";
+            corAmbienteEmpresa = emProducao ? "danger" : "warning";
+        }
+        catch (NfseSaaS.Application.Exceptions.RecursoNaoEncontradoException)
+        {
+            // Cookie aponta pra uma Empresa que não existe mais (Tenant
+            // errado, excluída) — empresa-atual.js detecta isso sozinho
+            // no próximo load (empresa some da lista) e corrige o
+            // cookie. Aqui só deixamos o badge em branco por essa carga.
+        }
+    }
 }
 <!DOCTYPE html>
 <html lang="pt-br">
@@ -96,9 +112,14 @@
                 </div>
 
                 <div class="d-flex align-items-center gap-2">
-                    <span class="badge text-bg-@corAmbienteSistema" title="Ambiente configurado em NfseNacional:BaseUrl no appsettings deste deploy">
-                        <i class="bi bi-hdd-network"></i> @rotuloAmbienteSistema
-                    </span>
+                    <select id="seletor-empresa-topo" class="form-select form-select-sm" style="width:auto" title="Empresa selecionada — vale pra todas as telas"></select>
+
+                    @if (rotuloAmbienteEmpresa is not null)
+                    {
+                        <span class="badge text-bg-@corAmbienteEmpresa" id="badge-ambiente-empresa" title="Ambiente da Empresa selecionada no topo">
+                            <i class="bi bi-hdd-network"></i> @rotuloAmbienteEmpresa
+                        </span>
+                    }
 
                     <button class="btn-tema" id="botaoTema" aria-label="Alternar tema claro/escuro" title="Alternar tema">
                         <i class="bi bi-moon-stars" id="iconeTema"></i>
@@ -158,6 +179,7 @@
     <script src="https://cdn.datatables.net/buttons/3.2.5/js/buttons.print.min.js"></script>
     <script src="https://cdnjs.cloudflare.com/ajax/libs/jszip/3.10.1/jszip.min.js"></script>
     <script src="~/js/site.js" asp-append-version="true"></script>
+    <script src="~/js/empresa-atual.js" asp-append-version="true"></script>
     <script src="~/js/exportacao.js" asp-append-version="true"></script>
 
     <script>
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Web/appsettings.Development.json NfseSaaS-master/src/NfseSaaS.Web/appsettings.Development.json
--- a/src/NfseSaaS.Web/appsettings.Development.json	2026-09-10 13:43:36.153953039 +0000
+++ b/src/NfseSaaS.Web/appsettings.Development.json	2026-09-10 13:32:32.196620419 +0000
@@ -5,11 +5,12 @@
     }
   },
   "NfseNacional": {
-    "BaseUrl": "https://sefin.producaorestrita.nfse.gov.br/SefinNacional/",
-    "Ambiente": "ProducaoRestrita"
+    "BaseUrlHomologacao": "https://sefin.producaorestrita.nfse.gov.br/SefinNacional/",
+    "BaseUrlProducao": ""
   },
   "Adn": {
-    "BaseUrl": "https://adn.producaorestrita.nfse.gov.br/contribuintes"
+    "BaseUrlHomologacao": "https://adn.producaorestrita.nfse.gov.br/contribuintes",
+    "BaseUrlProducao": ""
   },
   "CertificateStorage": {
     "BasePath": "C:\\NfseSaaS-Secrets\\certificados"
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Web/appsettings.json NfseSaaS-master/src/NfseSaaS.Web/appsettings.json
--- a/src/NfseSaaS.Web/appsettings.json	2026-09-10 13:43:36.154393639 +0000
+++ b/src/NfseSaaS.Web/appsettings.json	2026-09-10 13:32:39.780036488 +0000
@@ -9,12 +9,13 @@
     }
   },
   "NfseNacional": {
-    "BaseUrl": "",
-    "Ambiente": "Homologacao",
+    "BaseUrlHomologacao": "",
+    "BaseUrlProducao": "",
     "TimeoutSeconds": 60
   },
   "Adn": {
-    "BaseUrl": "",
+    "BaseUrlHomologacao": "",
+    "BaseUrlProducao": "",
     "TimeoutSeconds": 60
   },
   "CertificateStorage": {
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Web/wwwroot/js/empresa-atual.js NfseSaaS-master/src/NfseSaaS.Web/wwwroot/js/empresa-atual.js
--- a/src/NfseSaaS.Web/wwwroot/js/empresa-atual.js	1970-01-01 00:00:00.000000000 +0000
+++ b/src/NfseSaaS.Web/wwwroot/js/empresa-atual.js	2026-09-10 13:41:39.358430817 +0000
@@ -0,0 +1,80 @@
+/**
+ * Fonte única de qual Empresa está selecionada no momento. Substitui o
+ * antigo empresa-seletor.js, que vivia dentro de cada página e guardava
+ * a escolha em sessionStorage. Agora o <select> existe uma vez só, no
+ * topo (_Layout.cshtml), e a escolha fica num COOKIE — não
+ * sessionStorage — porque o Razor precisa conseguir ler o mesmo valor
+ * no servidor (pra montar o badge de ambiente sem esperar o JS rodar e
+ * sem "piscar" o valor errado por uma fração de segundo).
+ *
+ * O cookie não é sensível: só lembra qual Empresa está sendo olhada,
+ * não concede nenhuma permissão — o back-end continua validando tudo
+ * por Tenant/Role do jeito que já fazia, em cada endpoint.
+ */
+const NOME_COOKIE_EMPRESA_ATUAL = 'EmpresaAtualId';
+
+function obterEmpresaAtualId() {
+    const match = document.cookie.match(new RegExp('(?:^|; )' + NOME_COOKIE_EMPRESA_ATUAL + '=([^;]*)'));
+    return match ? decodeURIComponent(match[1]) : null;
+}
+
+function definirEmpresaAtualId(empresaId) {
+    const expira = new Date();
+    expira.setDate(expira.getDate() + 365);
+    document.cookie = `${NOME_COOKIE_EMPRESA_ATUAL}=${encodeURIComponent(empresaId)}; expires=${expira.toUTCString()}; path=/; SameSite=Lax`;
+}
+
+/**
+ * Roda em toda página (o <select id="seletor-empresa-topo"> vive no
+ * _Layout) — popula a lista de empresas, garante que sempre existe uma
+ * selecionada (cookie) e recarrega a página quando a seleção muda ou
+ * quando precisa resolver um valor novo (primeira visita, sem cookie
+ * ainda; ou a empresa do cookie não existe mais na lista, por exemplo
+ * foi excluída). Recarregar é deliberado: é o jeito mais simples de
+ * garantir que TODA a página — o badge server-side no topo e os dados
+ * carregados via JS mais abaixo — fique consistente com a nova escolha,
+ * sem precisar espalhar um mecanismo de "avisar todo mundo que mudou"
+ * entre scripts de páginas diferentes.
+ */
+async function inicializarSeletorEmpresaTopo() {
+    const select = document.getElementById('seletor-empresa-topo');
+    if (!select) return;
+
+    try {
+        const resultado = await apiFetch('/api/empresas?pageSize=200');
+        select.innerHTML = '';
+
+        if (!resultado.items.length) {
+            select.innerHTML = '<option value="">Nenhuma empresa cadastrada</option>';
+            return;
+        }
+
+        for (const empresa of resultado.items) {
+            const option = document.createElement('option');
+            option.value = empresa.id;
+            option.textContent = `${empresa.razaoSocial} (${empresa.cnpj})`;
+            select.appendChild(option);
+        }
+
+        let idAtual = obterEmpresaAtualId();
+        const existeNaLista = idAtual && resultado.items.some(e => e.id === idAtual);
+
+        if (!existeNaLista) {
+            idAtual = resultado.items[0].id;
+            definirEmpresaAtualId(idAtual);
+            location.reload();
+            return;
+        }
+
+        select.value = idAtual;
+
+        select.addEventListener('change', function () {
+            definirEmpresaAtualId(select.value);
+            location.reload();
+        });
+    } catch (err) {
+        mostrarErro(err.message);
+    }
+}
+
+document.addEventListener('DOMContentLoaded', inicializarSeletorEmpresaTopo);
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Web/wwwroot/js/empresa-seletor.js NfseSaaS-master/src/NfseSaaS.Web/wwwroot/js/empresa-seletor.js
--- a/src/NfseSaaS.Web/wwwroot/js/empresa-seletor.js	2026-09-10 13:43:36.155348451 +0000
+++ b/src/NfseSaaS.Web/wwwroot/js/empresa-seletor.js	1970-01-01 00:00:00.000000000 +0000
@@ -1,40 +0,0 @@
-/**
- * Clientes, Serviços e Nfse são todos escopados por Empresa na API
- * (um Tenant pode ter várias Empresas emissoras). Este helper povoa um
- * <select id="seletor-empresa"> com as Empresas do Tenant e lembra a
- * última escolhida em sessionStorage, pra não pedir de novo a cada
- * troca de tela dentro da mesma sessão do navegador.
- */
-async function inicializarSeletorEmpresa(aoTrocarEmpresa) {
-    const select = document.getElementById('seletor-empresa');
-
-    try {
-        const resultado = await apiFetch('/api/empresas?pageSize=200');
-        select.innerHTML = '';
-
-        if (!resultado.items.length) {
-            select.innerHTML = '<option value="">Nenhuma empresa cadastrada</option>';
-            return;
-        }
-
-        for (const empresa of resultado.items) {
-            const option = document.createElement('option');
-            option.value = empresa.id;
-            option.textContent = `${empresa.razaoSocial} (${empresa.cnpj})`;
-            select.appendChild(option);
-        }
-
-        const salvaId = sessionStorage.getItem('empresaSelecionadaId');
-        const existeNaLista = salvaId && resultado.items.some(e => e.id === salvaId);
-        select.value = existeNaLista ? salvaId : resultado.items[0].id;
-
-        select.addEventListener('change', function () {
-            sessionStorage.setItem('empresaSelecionadaId', select.value);
-            aoTrocarEmpresa(select.value);
-        });
-
-        aoTrocarEmpresa(select.value);
-    } catch (err) {
-        mostrarErro(err.message);
-    }
-}
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Web/wwwroot/js/paginas/clientes.js NfseSaaS-master/src/NfseSaaS.Web/wwwroot/js/paginas/clientes.js
--- a/src/NfseSaaS.Web/wwwroot/js/paginas/clientes.js	2026-09-10 13:43:36.156184026 +0000
+++ b/src/NfseSaaS.Web/wwwroot/js/paginas/clientes.js	2026-09-10 13:40:55.293592105 +0000
@@ -58,10 +58,8 @@
         });
     }
 
-    inicializarSeletorEmpresa(function (empresaId) {
-        empresaAtualId = empresaId;
-        carregarClientes();
-    });
+    empresaAtualId = obterEmpresaAtualId();
+    carregarClientes();
 });
 
 async function carregarClientes() {
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Web/wwwroot/js/paginas/contratos.js NfseSaaS-master/src/NfseSaaS.Web/wwwroot/js/paginas/contratos.js
--- a/src/NfseSaaS.Web/wwwroot/js/paginas/contratos.js	2026-09-10 13:43:36.156239452 +0000
+++ b/src/NfseSaaS.Web/wwwroot/js/paginas/contratos.js	2026-09-10 13:41:00.449832380 +0000
@@ -83,10 +83,8 @@
         });
     }
 
-    inicializarSeletorEmpresa(function (empresaId) {
-        empresaAtualIdContratos = empresaId;
-        carregarContratos();
-    });
+    empresaAtualIdContratos = obterEmpresaAtualId();
+    carregarContratos();
 });
 
 async function carregarContratos() {
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Web/wwwroot/js/paginas/empresas.js NfseSaaS-master/src/NfseSaaS.Web/wwwroot/js/paginas/empresas.js
--- a/src/NfseSaaS.Web/wwwroot/js/paginas/empresas.js	2026-09-10 13:43:36.167678594 +0000
+++ b/src/NfseSaaS.Web/wwwroot/js/paginas/empresas.js	2026-09-10 13:42:35.513103075 +0000
@@ -48,9 +48,12 @@
                     <i class="bi bi-file-earmark-lock"></i>
                 </button>
                 ${empresa.tipoAmbiente === AMBIENTE_HOMOLOGACAO ? `
-                <button type="button" class="btn btn-sm btn-outline-danger btn-promover-producao" data-id="${empresa.id}" data-nome="${empresa.razaoSocial}" title="Promover para Produção">
+                <button type="button" class="btn btn-sm btn-outline-danger btn-alterar-ambiente" data-id="${empresa.id}" data-nome="${empresa.razaoSocial}" data-novo-ambiente="${AMBIENTE_PRODUCAO}" title="Promover para Produção">
                     <i class="bi bi-rocket-takeoff"></i>
-                </button>` : ''}
+                </button>` : `
+                <button type="button" class="btn btn-sm btn-outline-secondary btn-alterar-ambiente" data-id="${empresa.id}" data-nome="${empresa.razaoSocial}" data-novo-ambiente="${AMBIENTE_HOMOLOGACAO}" title="Voltar para Homologação">
+                    <i class="bi bi-arrow-counterclockwise"></i>
+                </button>`}
                 <button type="button" class="btn btn-sm btn-outline-warning btn-alternar-ativo" data-id="${empresa.id}" data-ativo="${empresa.ativo}">
                     <i class="bi ${empresa.ativo ? 'bi-toggle-on' : 'bi-toggle-off'}"></i>
                 </button>
@@ -85,7 +88,7 @@
 
             if (botao.classList.contains('btn-editar')) await abrirModalEditarEmpresa(id);
             else if (botao.classList.contains('btn-certificado')) await abrirModalCertificado(id, botao.dataset.nome);
-            else if (botao.classList.contains('btn-promover-producao')) await promoverParaProducao(id, botao.dataset.nome);
+            else if (botao.classList.contains('btn-alterar-ambiente')) await alterarAmbiente(id, botao.dataset.nome, parseInt(botao.dataset.novoAmbiente, 10));
             else if (botao.classList.contains('btn-alternar-ativo')) await alternarAtivo(id, botao.dataset.ativo === 'true');
             else if (botao.classList.contains('btn-excluir')) await excluirEmpresa(id);
         });
@@ -217,17 +220,36 @@
     }
 }
 
-async function promoverParaProducao(id, nomeEmpresa) {
-    const confirmado = await confirmarAcao(
-        `Promover "${nomeEmpresa}" para Produção? A partir de agora, toda nota emitida por ela terá efeito fiscal real. Não é possível voltar para Homologação por aqui.`,
-        { titulo: 'Promover para Produção', textoBotao: 'Promover', variante: 'perigo' }
-    );
+async function alterarAmbiente(id, nomeEmpresa, novoAmbiente) {
+    const indoPraProducao = novoAmbiente === AMBIENTE_PRODUCAO;
+    const mensagem = indoPraProducao
+        ? `Promover "${nomeEmpresa}" para Produção? A partir de agora, toda nota emitida por ela terá efeito fiscal real.`
+        : `Voltar "${nomeEmpresa}" para Homologação? A partir de agora, as notas emitidas por ela voltam a ser de teste (sem efeito fiscal). Notas já emitidas em Produção continuam valendo — isso só afeta o que for emitido daqui pra frente.`;
+
+    const confirmado = await confirmarAcao(mensagem, {
+        titulo: indoPraProducao ? 'Promover para Produção' : 'Voltar para Homologação',
+        textoBotao: indoPraProducao ? 'Promover' : 'Voltar',
+        variante: 'perigo'
+    });
     if (!confirmado) return;
 
     try {
-        await apiFetch(`/api/empresas/${id}/promover-producao`, { method: 'POST' });
+        await apiFetch(`/api/empresas/${id}/ambiente`, {
+            method: 'PUT',
+            body: JSON.stringify({ tipoAmbiente: novoAmbiente })
+        });
+
+        // Se a empresa alterada é a que está selecionada no topo, o
+        // badge lá em cima também precisa refletir o ambiente novo —
+        // recarregar é o jeito mais simples de manter tudo consistente
+        // (mesmo raciocínio de empresa-atual.js ao trocar a seleção).
+        if (id === obterEmpresaAtualId()) {
+            location.reload();
+            return;
+        }
+
         await carregarEmpresas();
-        mostrarToast(`"${nomeEmpresa}" agora está em Produção.`);
+        mostrarToast(`"${nomeEmpresa}" agora está em ${indoPraProducao ? 'Produção' : 'Homologação'}.`);
     } catch (err) {
         mostrarErro(err.message);
     }
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Web/wwwroot/js/paginas/nfse.js NfseSaaS-master/src/NfseSaaS.Web/wwwroot/js/paginas/nfse.js
--- a/src/NfseSaaS.Web/wwwroot/js/paginas/nfse.js	2026-09-10 13:43:36.156686457 +0000
+++ b/src/NfseSaaS.Web/wwwroot/js/paginas/nfse.js	2026-09-10 13:41:05.867446742 +0000
@@ -73,11 +73,11 @@
         document.getElementById('form-cancelar-nfse').addEventListener('submit', confirmarCancelamentoNfse);
     }
 
-    inicializarSeletorEmpresa(async function (empresaId) {
-        empresaAtualIdNfse = empresaId;
+    empresaAtualIdNfse = obterEmpresaAtualId();
+    (async function () {
         await carregarClientesParaMapa();
         await carregarNfse();
-    });
+    })();
 });
 
 async function carregarClientesParaMapa() {
diff -ruN '--exclude=.git' baseline-patch1/src/NfseSaaS.Web/wwwroot/js/paginas/servicos.js NfseSaaS-master/src/NfseSaaS.Web/wwwroot/js/paginas/servicos.js
--- a/src/NfseSaaS.Web/wwwroot/js/paginas/servicos.js	2026-09-10 13:43:36.156850650 +0000
+++ b/src/NfseSaaS.Web/wwwroot/js/paginas/servicos.js	2026-09-10 13:41:02.903835185 +0000
@@ -59,10 +59,8 @@
         });
     }
 
-    inicializarSeletorEmpresa(function (empresaId) {
-        empresaAtualIdServicos = empresaId;
-        carregarServicos();
-    });
+    empresaAtualIdServicos = obterEmpresaAtualId();
+    carregarServicos();
 });
 
 async function carregarServicos() {
diff -ruN '--exclude=.git' baseline-patch1/tests/NfseSaaS.Tests/NfseNacionalOptionsTests.cs NfseSaaS-master/tests/NfseSaaS.Tests/NfseNacionalOptionsTests.cs
--- a/tests/NfseSaaS.Tests/NfseNacionalOptionsTests.cs	2026-09-10 13:43:36.158275345 +0000
+++ b/tests/NfseSaaS.Tests/NfseNacionalOptionsTests.cs	2026-09-10 13:32:50.799870600 +0000
@@ -10,8 +10,22 @@
     {
         var options = new NfseNacionalOptions();
 
-        Assert.Equal("Homologacao", options.Ambiente);
+        Assert.Equal(string.Empty, options.BaseUrlHomologacao);
+        Assert.Equal(string.Empty, options.BaseUrlProducao);
         Assert.Equal(60, options.TimeoutSeconds);
-        Assert.Equal(string.Empty, options.BaseUrl);
+    }
+
+    [Theory]
+    [InlineData("1", "https://producao.exemplo/")]
+    [InlineData("2", "https://homologacao.exemplo/")]
+    public void ObterBaseUrl_deve_escolher_a_url_certa_pelo_tpAmb(string tpAmb, string urlEsperada)
+    {
+        var options = new NfseNacionalOptions
+        {
+            BaseUrlProducao = "https://producao.exemplo/",
+            BaseUrlHomologacao = "https://homologacao.exemplo/"
+        };
+
+        Assert.Equal(urlEsperada, options.ObterBaseUrl(tpAmb));
     }
 }
diff -ruN '--exclude=.git' baseline-patch1/tools/NfseSaaS.EmitirTeste/Program.cs NfseSaaS-master/tools/NfseSaaS.EmitirTeste/Program.cs
--- a/tools/NfseSaaS.EmitirTeste/Program.cs	2026-09-10 13:43:36.168308955 +0000
+++ b/tools/NfseSaaS.EmitirTeste/Program.cs	2026-09-10 13:32:44.869577442 +0000
@@ -46,8 +46,12 @@
 var configuration = new ConfigurationBuilder()
     .AddInMemoryCollection(new Dictionary<string, string?>
     {
-        ["NfseNacional:BaseUrl"] = baseUrl,
-        ["NfseNacional:Ambiente"] = ambiente,
+        // Ferramenta manual recebe uma única URL por execução (o
+        // [ambiente] da linha de comando já diz qual é) — preenche as
+        // duas chaves com o mesmo valor de propósito; NfseApiClient só
+        // vai de fato usar a que corresponder ao tpAmb calculado abaixo.
+        ["NfseNacional:BaseUrlHomologacao"] = baseUrl,
+        ["NfseNacional:BaseUrlProducao"] = baseUrl,
         ["NfseNacional:TimeoutSeconds"] = "60"
     })
     .Build();
