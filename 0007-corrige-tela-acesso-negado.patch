diff --git a/src/NfseSaaS.Web/Controllers/AccountController.cs b/src/NfseSaaS.Web/Controllers/AccountController.cs
index 20f3e7c..8b66478 100644
--- a/src/NfseSaaS.Web/Controllers/AccountController.cs
+++ b/src/NfseSaaS.Web/Controllers/AccountController.cs
@@ -34,4 +34,15 @@ public sealed class AccountController : Controller
         ViewData["Token"] = token;
         return View();
     }
+
+    /// <summary>
+    /// Destino padrão do Identity (AccessDeniedPath) quando um usuário
+    /// autenticado tenta uma tela MVC sem permissão — rota que faltava
+    /// no projeto (só existia pra /api, tratado à parte em
+    /// OnRedirectToAccessDenied) e nunca tinha sido exercitada até o
+    /// módulo de segurança IAEC ([RequerPermissao]) tornar esse caminho
+    /// alcançável de verdade.
+    /// </summary>
+    [HttpGet("/Account/AccessDenied")]
+    public IActionResult AccessDenied(string? returnUrl = null) => View();
 }
diff --git a/src/NfseSaaS.Web/Views/Account/AccessDenied.cshtml b/src/NfseSaaS.Web/Views/Account/AccessDenied.cshtml
new file mode 100644
index 0000000..fe7e5b5
--- /dev/null
+++ b/src/NfseSaaS.Web/Views/Account/AccessDenied.cshtml
@@ -0,0 +1,18 @@
+@{
+    ViewData["Title"] = "Acesso negado";
+}
+
+<div class="card">
+    <div class="card-body text-center py-5">
+        <i class="bi bi-shield-lock display-1 text-danger"></i>
+        <h4 class="mt-3">Você não tem permissão pra acessar esta tela</h4>
+        <p class="text-muted">
+            Se você acha que deveria ter acesso, fale com um Administrador do seu Tenant —
+            ele pode ajustar o seu grupo de permissão na tela de Usuários.
+        </p>
+        <p class="text-muted small">
+            Se a sua permissão acabou de ser alterada, você precisa sair e entrar de novo pra ela valer.
+        </p>
+        <a href="/" class="btn btn-primary">Voltar ao início</a>
+    </div>
+</div>
