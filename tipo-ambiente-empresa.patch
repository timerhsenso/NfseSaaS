diff -ruN '--exclude=.git' orig/NfseSaaS-master/src/NfseSaaS.Application/UseCases/Empresas/EmpresaResponse.cs NfseSaaS-master/src/NfseSaaS.Application/UseCases/Empresas/EmpresaResponse.cs
--- a/src/NfseSaaS.Application/UseCases/Empresas/EmpresaResponse.cs	2026-09-10 02:39:34.000000000 +0000
+++ b/src/NfseSaaS.Application/UseCases/Empresas/EmpresaResponse.cs	2026-09-10 11:46:32.191179501 +0000
@@ -1,3 +1,5 @@
+using NfseSaaS.Domain.Enums;
+
 namespace NfseSaaS.Application.UseCases.Empresas;
 
 public sealed record EmpresaResponse(
@@ -25,5 +27,9 @@
     string PercentualTotalTributosSimplesNacional,
     int DiasAlertaReajusteContratoPadrao,
     bool Ativo,
+    // Serializa como int (1/2), mesmo padrão já usado por NfseStatus em
+    // NfseResponse — sem JsonStringEnumConverter configurado no projeto.
+    // O front traduz pra rótulo, igual ROTULOS_STATUS já faz em nfse.js.
+    TipoAmbiente TipoAmbiente,
     DateTimeOffset CreatedAt,
     DateTimeOffset? UpdatedAt);
diff -ruN '--exclude=.git' orig/NfseSaaS-master/src/NfseSaaS.Application/UseCases/Empresas/IPromoverEmpresaParaProducaoUseCase.cs NfseSaaS-master/src/NfseSaaS.Application/UseCases/Empresas/IPromoverEmpresaParaProducaoUseCase.cs
--- a/src/NfseSaaS.Application/UseCases/Empresas/IPromoverEmpresaParaProducaoUseCase.cs	1970-01-01 00:00:00.000000000 +0000
+++ b/src/NfseSaaS.Application/UseCases/Empresas/IPromoverEmpresaParaProducaoUseCase.cs	2026-09-10 11:46:14.474580457 +0000
@@ -0,0 +1,6 @@
+namespace NfseSaaS.Application.UseCases.Empresas;
+
+public interface IPromoverEmpresaParaProducaoUseCase
+{
+    Task ExecutarAsync(Guid id, CancellationToken cancellationToken);
+}
diff -ruN '--exclude=.git' orig/NfseSaaS-master/src/NfseSaaS.Domain/Entities/Empresa.cs NfseSaaS-master/src/NfseSaaS.Domain/Entities/Empresa.cs
--- a/src/NfseSaaS.Domain/Entities/Empresa.cs	2026-09-10 02:39:34.000000000 +0000
+++ b/src/NfseSaaS.Domain/Entities/Empresa.cs	2026-09-10 11:45:15.390539002 +0000
@@ -1,4 +1,5 @@
 using NfseSaaS.Domain.Common;
+using NfseSaaS.Domain.Enums;
 
 namespace NfseSaaS.Domain.Entities;
 
@@ -67,6 +68,15 @@
     public bool Ativo { get; set; } = true;
 
     /// <summary>
+    /// Em que ambiente do SEFIN Nacional as notas desta Empresa são
+    /// emitidas (vira o tpAmb da DPS — ver DpsBuilder). Toda Empresa
+    /// nasce em Homologacao; a promoção para Producao é deliberada e
+    /// auditada (ver IPromoverEmpresaParaProducaoUseCase), nunca o
+    /// valor default.
+    /// </summary>
+    public TipoAmbiente TipoAmbiente { get; set; } = TipoAmbiente.Homologacao;
+
+    /// <summary>
     /// Último NSU (Número Sequencial Único) já processado na sincronização
     /// com o ADN (Distribuição de DF-e) — ver SincronizarNotasDaSefinUseCase.
     /// Null/0 = nunca sincronizado, próxima chamada começa do zero.
diff -ruN '--exclude=.git' orig/NfseSaaS-master/src/NfseSaaS.Domain/Enums/TipoAmbiente.cs NfseSaaS-master/src/NfseSaaS.Domain/Enums/TipoAmbiente.cs
--- a/src/NfseSaaS.Domain/Enums/TipoAmbiente.cs	1970-01-01 00:00:00.000000000 +0000
+++ b/src/NfseSaaS.Domain/Enums/TipoAmbiente.cs	2026-09-10 11:45:08.670576545 +0000
@@ -0,0 +1,20 @@
+namespace NfseSaaS.Domain.Enums;
+
+/// <summary>
+/// Em que ambiente do SEFIN Nacional as notas de uma Empresa devem ser
+/// tratadas como válidas. Valores numéricos iguais ao campo tpAmb da DPS
+/// (1 = Produção, 2 = Homologação) — sem tradução em runtime entre este
+/// enum e o XML.
+///
+/// IMPORTANTE: isso é independente da URL do SEFIN configurada no
+/// appsettings (NfseNacionalOptions.BaseUrl) — essa URL é única e igual
+/// para todas as Empresas do sistema. O SEFIN Nacional aceita tpAmb=2
+/// (nota de teste, sem efeito fiscal) no MESMO endpoint usado pra
+/// tpAmb=1 — por isso uma Empresa nova pode ser testada em Homologação
+/// sem nenhuma mudança de configuração global.
+/// </summary>
+public enum TipoAmbiente
+{
+    Producao = 1,
+    Homologacao = 2
+}
diff -ruN '--exclude=.git' orig/NfseSaaS-master/src/NfseSaaS.Infrastructure/DependencyInjection.cs NfseSaaS-master/src/NfseSaaS.Infrastructure/DependencyInjection.cs
--- a/src/NfseSaaS.Infrastructure/DependencyInjection.cs	2026-09-10 02:39:34.000000000 +0000
+++ b/src/NfseSaaS.Infrastructure/DependencyInjection.cs	2026-09-10 11:46:25.555350254 +0000
@@ -116,6 +116,7 @@
         services.AddScoped<IDesativarEmpresaUseCase, DesativarEmpresaUseCase>();
         services.AddScoped<IReativarEmpresaUseCase, ReativarEmpresaUseCase>();
         services.AddScoped<IExcluirEmpresaUseCase, ExcluirEmpresaUseCase>();
+        services.AddScoped<IPromoverEmpresaParaProducaoUseCase, PromoverEmpresaParaProducaoUseCase>();
 
         services.AddScoped<ICadastrarClienteUseCase, CadastrarClienteUseCase>();
         services.AddScoped<IListarClientesUseCase, ListarClientesUseCase>();
diff -ruN '--exclude=.git' orig/NfseSaaS-master/src/NfseSaaS.Infrastructure/Persistence/Configurations/EmpresaConfiguration.cs NfseSaaS-master/src/NfseSaaS.Infrastructure/Persistence/Configurations/EmpresaConfiguration.cs
--- a/src/NfseSaaS.Infrastructure/Persistence/Configurations/EmpresaConfiguration.cs	2026-09-10 02:39:34.000000000 +0000
+++ b/src/NfseSaaS.Infrastructure/Persistence/Configurations/EmpresaConfiguration.cs	2026-09-10 11:45:22.355045834 +0000
@@ -1,6 +1,7 @@
 using Microsoft.EntityFrameworkCore;
 using Microsoft.EntityFrameworkCore.Metadata.Builders;
 using NfseSaaS.Domain.Entities;
+using NfseSaaS.Domain.Enums;
 
 namespace NfseSaaS.Infrastructure.Persistence.Configurations;
 
@@ -81,6 +82,14 @@
         builder.Property(e => e.PercentualTotalTributosSimplesNacional)
             .HasMaxLength(10);
 
+        // Grava como int (mesmo valor do tpAmb — ver TipoAmbiente). Default
+        // explícito no banco (não só no C#) por segurança: qualquer INSERT
+        // que por algum motivo não passe pelo EF Core (script manual,
+        // outra ferramenta) ainda cai em Homologacao, nunca em Producao.
+        builder.Property(e => e.TipoAmbiente)
+            .HasConversion<int>()
+            .HasDefaultValue(TipoAmbiente.Homologacao);
+
         // Um mesmo CNPJ não pode se repetir dentro do mesmo tenant.
         builder.HasIndex(e => new { e.TenantId, e.Cnpj })
             .IsUnique();
diff -ruN '--exclude=.git' orig/NfseSaaS-master/src/NfseSaaS.Infrastructure/UseCases/CancelarNfseUseCase.cs NfseSaaS-master/src/NfseSaaS.Infrastructure/UseCases/CancelarNfseUseCase.cs
--- a/src/NfseSaaS.Infrastructure/UseCases/CancelarNfseUseCase.cs	2026-09-10 02:39:34.000000000 +0000
+++ b/src/NfseSaaS.Infrastructure/UseCases/CancelarNfseUseCase.cs	2026-09-10 11:53:13.041030874 +0000
@@ -56,7 +56,8 @@
             ChaveAcesso: nfse.ChaveAcesso,
             CnpjAutor: empresa.Cnpj,
             CodigoMotivo: request.CodigoMotivo,
-            Motivo: request.Motivo);
+            Motivo: request.Motivo,
+            TpAmb: empresa.TipoAmbiente == TipoAmbiente.Producao ? "1" : "2");
 
         var resposta = await _nfseNacionalService.CancelarAsync(eventoRequest, empresa.Id, cancellationToken);
 
diff -ruN '--exclude=.git' orig/NfseSaaS-master/src/NfseSaaS.Infrastructure/UseCases/EmitirNfseUseCase.cs NfseSaaS-master/src/NfseSaaS.Infrastructure/UseCases/EmitirNfseUseCase.cs
--- a/src/NfseSaaS.Infrastructure/UseCases/EmitirNfseUseCase.cs	2026-09-10 02:39:34.000000000 +0000
+++ b/src/NfseSaaS.Infrastructure/UseCases/EmitirNfseUseCase.cs	2026-09-10 11:46:11.388211294 +0000
@@ -182,7 +182,12 @@
             Valor: request.ValorServico,
             CodigoTributacaoNacional: nfse.CodigoTributacaoNacional,
             CodigoNbs: nfse.CodigoNbs,
-            DescricaoServico: request.DescricaoServico);
+            DescricaoServico: request.DescricaoServico,
+            // Tradução Domain → Nacional acontece só aqui, no único lugar
+            // que conhece os dois lados (ver comentário em DpsRequest.TpAmb
+            // sobre por que o módulo Nacional não pode fazer essa tradução
+            // sozinho).
+            TpAmb: empresa.TipoAmbiente == TipoAmbiente.Producao ? "1" : "2");
 
         try
         {
diff -ruN '--exclude=.git' orig/NfseSaaS-master/src/NfseSaaS.Infrastructure/UseCases/ListarEmpresasUseCase.cs NfseSaaS-master/src/NfseSaaS.Infrastructure/UseCases/ListarEmpresasUseCase.cs
--- a/src/NfseSaaS.Infrastructure/UseCases/ListarEmpresasUseCase.cs	2026-09-10 02:39:34.000000000 +0000
+++ b/src/NfseSaaS.Infrastructure/UseCases/ListarEmpresasUseCase.cs	2026-09-10 11:46:38.770804026 +0000
@@ -40,7 +40,7 @@
                 e.Telefone, e.Email, e.Cep, e.Logradouro, e.Numero, e.Complemento, e.Bairro, e.Uf,
                 e.OpSimpNac, e.RegApTribSN, e.RegEspTrib, e.TribIssqn, e.TpRetIssqn,
                 e.CstPisCofins, e.TpRetPisCofins, e.PercentualTotalTributosSimplesNacional,
-                e.DiasAlertaReajusteContratoPadrao, e.Ativo, e.CreatedAt, e.UpdatedAt))
+                e.DiasAlertaReajusteContratoPadrao, e.Ativo, e.TipoAmbiente, e.CreatedAt, e.UpdatedAt))
             .ToListAsync(cancellationToken);
 
         return new PagedResult<EmpresaResponse>
diff -ruN '--exclude=.git' orig/NfseSaaS-master/src/NfseSaaS.Infrastructure/UseCases/ObterEmpresaPorIdUseCase.cs NfseSaaS-master/src/NfseSaaS.Infrastructure/UseCases/ObterEmpresaPorIdUseCase.cs
--- a/src/NfseSaaS.Infrastructure/UseCases/ObterEmpresaPorIdUseCase.cs	2026-09-10 02:39:34.000000000 +0000
+++ b/src/NfseSaaS.Infrastructure/UseCases/ObterEmpresaPorIdUseCase.cs	2026-09-10 11:46:41.930567461 +0000
@@ -27,6 +27,6 @@
             empresa.Numero, empresa.Complemento, empresa.Bairro, empresa.Uf, empresa.OpSimpNac, empresa.RegApTribSN,
             empresa.RegEspTrib, empresa.TribIssqn, empresa.TpRetIssqn, empresa.CstPisCofins, empresa.TpRetPisCofins,
             empresa.PercentualTotalTributosSimplesNacional, empresa.DiasAlertaReajusteContratoPadrao,
-            empresa.Ativo, empresa.CreatedAt, empresa.UpdatedAt);
+            empresa.Ativo, empresa.TipoAmbiente, empresa.CreatedAt, empresa.UpdatedAt);
     }
 }
diff -ruN '--exclude=.git' orig/NfseSaaS-master/src/NfseSaaS.Infrastructure/UseCases/PromoverEmpresaParaProducaoUseCase.cs NfseSaaS-master/src/NfseSaaS.Infrastructure/UseCases/PromoverEmpresaParaProducaoUseCase.cs
--- a/src/NfseSaaS.Infrastructure/UseCases/PromoverEmpresaParaProducaoUseCase.cs	1970-01-01 00:00:00.000000000 +0000
+++ b/src/NfseSaaS.Infrastructure/UseCases/PromoverEmpresaParaProducaoUseCase.cs	2026-09-10 11:46:22.674580944 +0000
@@ -0,0 +1,51 @@
+using Microsoft.EntityFrameworkCore;
+using NfseSaaS.Application.Abstractions;
+using NfseSaaS.Application.Exceptions;
+using NfseSaaS.Application.UseCases.Empresas;
+using NfseSaaS.Domain.Enums;
+using NfseSaaS.Infrastructure.Persistence;
+
+namespace NfseSaaS.Infrastructure.UseCases;
+
+/// <summary>
+/// Promove uma Empresa de Homologação para Produção — ação deliberada e
+/// UNIDIRECIONAL: não existe "rebaixar de volta pra Homologação" por
+/// aqui. Se isso um dia for realmente necessário (empresa promovida por
+/// engano, por exemplo), é uma decisão grande o suficiente pra passar
+/// por uma alteração direta no banco com registro manual do motivo, não
+/// por um botão na tela — o mesmo raciocínio de "sem bypass, sem
+/// gambiarra" aplicado a um campo que decide se a próxima nota emitida
+/// tem efeito fiscal real.
+///
+/// Idempotente: se a Empresa já está em Produção, não faz nada e não
+/// gera entrada de auditoria — nada mudou de fato (mesmo padrão de
+/// DesativarEmpresaUseCase).
+/// </summary>
+public sealed class PromoverEmpresaParaProducaoUseCase : IPromoverEmpresaParaProducaoUseCase
+{
+    private readonly AppDbContext _db;
+    private readonly IAuditLogWriter _auditLogWriter;
+
+    public PromoverEmpresaParaProducaoUseCase(AppDbContext db, IAuditLogWriter auditLogWriter)
+    {
+        _db = db;
+        _auditLogWriter = auditLogWriter;
+    }
+
+    public async Task ExecutarAsync(Guid id, CancellationToken cancellationToken)
+    {
+        var empresa = await _db.Empresas.FirstOrDefaultAsync(e => e.Id == id, cancellationToken);
+
+        if (empresa is null)
+            throw new RecursoNaoEncontradoException($"Empresa {id} não encontrada.");
+
+        if (empresa.TipoAmbiente == TipoAmbiente.Producao)
+            return;
+
+        empresa.TipoAmbiente = TipoAmbiente.Producao;
+
+        _auditLogWriter.Registrar("PromoverEmpresaParaProducao", "Empresa", empresa.Id, new { empresa.RazaoSocial, empresa.Cnpj });
+
+        await _db.SaveChangesAsync(cancellationToken);
+    }
+}
diff -ruN '--exclude=.git' orig/NfseSaaS-master/src/NfseSaaS.Nacional/Builders/DpsBuilder.cs NfseSaaS-master/src/NfseSaaS.Nacional/Builders/DpsBuilder.cs
--- a/src/NfseSaaS.Nacional/Builders/DpsBuilder.cs	2026-09-10 02:39:34.000000000 +0000
+++ b/src/NfseSaaS.Nacional/Builders/DpsBuilder.cs	2026-09-10 11:46:06.812240677 +0000
@@ -1,9 +1,7 @@
 using System.Globalization;
 using System.Xml;
-using Microsoft.Extensions.Options;
 using NfseSaaS.Nacional.Helpers;
 using NfseSaaS.Nacional.Models;
-using NfseSaaS.Nacional.Options;
 
 namespace NfseSaaS.Nacional.Builders;
 
@@ -17,6 +15,12 @@
 /// tomador e tributação agora vêm de <see cref="DpsRequest"/> — a POC
 /// tinha um único prestador fixo no código, o que não faz sentido num SaaS
 /// multiempresa.
+///
+/// O tpAmb (request.TpAmb) TAMBÉM vem de fora agora — este builder não
+/// lê mais NfseNacionalOptions/appsettings pra decidir isso. Antes era
+/// um valor global (mesmo ambiente fiscal pra toda empresa do sistema);
+/// hoje é decidido por Empresa (Empresa.TipoAmbiente), resolvido em
+/// EmitirNfseUseCase antes de chegar aqui.
 /// </summary>
 public sealed class DpsBuilder : IDpsBuilder
 {
@@ -38,15 +42,11 @@
     // processamento").
     private static readonly TimeSpan MargemSegurancaDhEmi = TimeSpan.FromSeconds(5);
 
-    private readonly NfseNacionalOptions _options;
-
-    public DpsBuilder(IOptions<NfseNacionalOptions> options)
-    {
-        _options = options.Value;
-    }
-
     public (string XmlDps, string InfDpsId) Construir(DpsRequest request)
     {
+        if (request.TpAmb is not ("1" or "2"))
+            throw new ArgumentException($"TpAmb inválido: '{request.TpAmb}'. Esperado \"1\" (Produção) ou \"2\" (Homologação).", nameof(request));
+
         var agora = DateTimeOffset.Now - MargemSegurancaDhEmi;
 
         var infDpsId =
@@ -67,7 +67,7 @@
         infDps.SetAttribute("Id", infDpsId);
         dps.AppendChild(infDps);
 
-        Add(doc, infDps, "tpAmb", MapearTpAmb(_options.Ambiente));
+        Add(doc, infDps, "tpAmb", request.TpAmb);
         Add(doc, infDps, "dhEmi", agora.ToString("yyyy-MM-ddTHH:mm:sszzz"));
         Add(doc, infDps, "verAplic", "NfseSaaS_1.0");
         Add(doc, infDps, "serie", request.SerieDps);
@@ -128,19 +128,6 @@
         return (XmlSerializationHelper.ToXmlString(doc), infDpsId);
     }
 
-    /// <summary>
-    /// tpAmb: 1 = Produção, 2 = Homologação. "ProducaoRestrita" (ambiente
-    /// de testes da SEFIN usado na POC) também mapeia para 2 — foi o valor
-    /// usado na emissão real que obteve HTTP 201.
-    /// </summary>
-    private static string MapearTpAmb(string ambiente) => ambiente switch
-    {
-        "Producao" => "1",
-        "Homologacao" => "2",
-        "ProducaoRestrita" => "2",
-        _ => "2"
-    };
-
     private static XmlElement AddNode(XmlDocument doc, XmlElement parent, string nome)
     {
         var element = doc.CreateElement(nome, Ns);
diff -ruN '--exclude=.git' orig/NfseSaaS-master/src/NfseSaaS.Nacional/Builders/EventoCancelamentoBuilder.cs NfseSaaS-master/src/NfseSaaS.Nacional/Builders/EventoCancelamentoBuilder.cs
--- a/src/NfseSaaS.Nacional/Builders/EventoCancelamentoBuilder.cs	2026-09-10 02:39:34.000000000 +0000
+++ b/src/NfseSaaS.Nacional/Builders/EventoCancelamentoBuilder.cs	2026-09-10 11:53:09.101025273 +0000
@@ -1,8 +1,6 @@
-using Microsoft.Extensions.Options;
 using System.Xml;
 using NfseSaaS.Nacional.Helpers;
 using NfseSaaS.Nacional.Models;
-using NfseSaaS.Nacional.Options;
 
 namespace NfseSaaS.Nacional.Builders;
 
@@ -33,15 +31,11 @@
     // municipais conveniados — não se aplica a este SaaS.
     private const string TipoEvento = "101101";
 
-    private readonly NfseNacionalOptions _options;
-
-    public EventoCancelamentoBuilder(IOptions<NfseNacionalOptions> options)
-    {
-        _options = options.Value;
-    }
-
     public (string XmlEvento, string InfPedRegId) Construir(EventoCancelamentoRequest request)
     {
+        if (request.TpAmb is not ("1" or "2"))
+            throw new ArgumentException($"TpAmb inválido: '{request.TpAmb}'. Esperado \"1\" (Produção) ou \"2\" (Homologação).", nameof(request));
+
         // Id = "PRE" + chNFSe (50) + tipoEvento (6) = 59 caracteres.
         // NÃO inclui mais nPedRegEvento (removido do leiaute em
         // 27/12/2025) — confirmado contra o padrão oficial
@@ -64,7 +58,7 @@
         // Ordem dos campos importa — infPedReg é um "sequence" no XSD,
         // e enviar fora de ordem já causou rejeição em outros sistemas
         // (mesma NT de 27/12/2025).
-        Add(doc, infPedReg, "tpAmb", MapearTpAmb(_options.Ambiente));
+        Add(doc, infPedReg, "tpAmb", request.TpAmb);
         Add(doc, infPedReg, "verAplic", "NfseSaaS_1.0");
         Add(doc, infPedReg, "dhEvento", agora.ToString("yyyy-MM-ddTHH:mm:sszzz"));
         Add(doc, infPedReg, "CNPJAutor", request.CnpjAutor);
@@ -78,14 +72,6 @@
         return (XmlSerializationHelper.ToXmlString(doc), infPedRegId);
     }
 
-    private static string MapearTpAmb(string ambiente) => ambiente switch
-    {
-        "Producao" => "1",
-        "Homologacao" => "2",
-        "ProducaoRestrita" => "2",
-        _ => "2"
-    };
-
     private static XmlElement AddNode(XmlDocument doc, XmlElement parent, string nome)
     {
         var element = doc.CreateElement(nome, Ns);
diff -ruN '--exclude=.git' orig/NfseSaaS-master/src/NfseSaaS.Nacional/Models/DpsRequest.cs NfseSaaS-master/src/NfseSaaS.Nacional/Models/DpsRequest.cs
--- a/src/NfseSaaS.Nacional/Models/DpsRequest.cs	2026-09-10 02:39:34.000000000 +0000
+++ b/src/NfseSaaS.Nacional/Models/DpsRequest.cs	2026-09-10 11:45:44.179272980 +0000
@@ -45,6 +45,13 @@
 /// NfseSaaS.Nacional — deliberadamente não é a entidade Nfse do Domain,
 /// para manter este módulo isolado (ver Requisito 4/5 da especificação).
 /// </summary>
+/// <param name="TpAmb">
+/// tpAmb da DPS: "1" = Produção, "2" = Homologação. Deliberadamente uma
+/// string pronta pro XML, não um enum do Domain — este módulo Nacional
+/// não conhece o Domain (ver arquitetura). Quem decide o valor é o
+/// chamador (EmitirNfseUseCase, a partir de Empresa.TipoAmbiente), nunca
+/// este módulo nem o appsettings.
+/// </param>
 public sealed record DpsRequest(
     PrestadorDps Prestador,
     TomadorDps Tomador,
@@ -55,4 +62,5 @@
     decimal Valor,
     string CodigoTributacaoNacional,
     string CodigoNbs,
-    string DescricaoServico);
\ No newline at end of file
+    string DescricaoServico,
+    string TpAmb);
\ No newline at end of file
diff -ruN '--exclude=.git' orig/NfseSaaS-master/src/NfseSaaS.Nacional/Models/EventoCancelamentoRequest.cs NfseSaaS-master/src/NfseSaaS.Nacional/Models/EventoCancelamentoRequest.cs
--- a/src/NfseSaaS.Nacional/Models/EventoCancelamentoRequest.cs	2026-09-10 02:39:34.000000000 +0000
+++ b/src/NfseSaaS.Nacional/Models/EventoCancelamentoRequest.cs	2026-09-10 11:52:49.153383553 +0000
@@ -5,8 +5,14 @@
 /// (e101101) de uma NFS-e já autorizada, leiaute pedRegEvento/infPedReg
 /// da SEFIN Nacional (schema 1.00).
 /// </summary>
+/// <param name="TpAmb">
+/// tpAmb do evento: "1" = Produção, "2" = Homologação — mesma regra e
+/// mesmo motivo do DpsRequest.TpAmb: precisa ser o ambiente da Empresa
+/// dona da nota sendo cancelada, não um valor global do appsettings.
+/// </param>
 public sealed record EventoCancelamentoRequest(
     string ChaveAcesso,
     string CnpjAutor,
     int CodigoMotivo,
-    string Motivo);
+    string Motivo,
+    string TpAmb);
diff -ruN '--exclude=.git' orig/NfseSaaS-master/src/NfseSaaS.Web/Controllers/Api/EmpresasController.cs NfseSaaS-master/src/NfseSaaS.Web/Controllers/Api/EmpresasController.cs
--- a/src/NfseSaaS.Web/Controllers/Api/EmpresasController.cs	2026-09-10 02:39:34.000000000 +0000
+++ b/src/NfseSaaS.Web/Controllers/Api/EmpresasController.cs	2026-09-10 11:46:54.259179272 +0000
@@ -19,6 +19,7 @@
     private readonly IDesativarEmpresaUseCase _desativarEmpresa;
     private readonly IReativarEmpresaUseCase _reativarEmpresa;
     private readonly IExcluirEmpresaUseCase _excluirEmpresa;
+    private readonly IPromoverEmpresaParaProducaoUseCase _promoverParaProducao;
     private readonly IEnviarCertificadoUseCase _enviarCertificado;
     private readonly IObterStatusCertificadoUseCase _obterStatusCertificado;
     private readonly ITestarConexaoCertificadoUseCase _testarConexaoCertificado;
@@ -32,6 +33,7 @@
         IDesativarEmpresaUseCase desativarEmpresa,
         IReativarEmpresaUseCase reativarEmpresa,
         IExcluirEmpresaUseCase excluirEmpresa,
+        IPromoverEmpresaParaProducaoUseCase promoverParaProducao,
         IEnviarCertificadoUseCase enviarCertificado,
         IObterStatusCertificadoUseCase obterStatusCertificado,
         ITestarConexaoCertificadoUseCase testarConexaoCertificado,
@@ -44,6 +46,7 @@
         _desativarEmpresa = desativarEmpresa;
         _reativarEmpresa = reativarEmpresa;
         _excluirEmpresa = excluirEmpresa;
+        _promoverParaProducao = promoverParaProducao;
         _enviarCertificado = enviarCertificado;
         _obterStatusCertificado = obterStatusCertificado;
         _testarConexaoCertificado = testarConexaoCertificado;
@@ -112,6 +115,21 @@
         return NoContent();
     }
 
+    /// <summary>
+    /// Promove a Empresa de Homologação para Produção — a partir daqui,
+    /// toda nova Nfse emitida por ela vai com tpAmb=1 (efeito fiscal
+    /// real). Ação unidirecional e auditada — não existe endpoint pra
+    /// voltar. Idempotente: chamar de novo numa Empresa já em Produção
+    /// não faz nada (204 do mesmo jeito).
+    /// </summary>
+    [Authorize(Roles = Papeis.Administrador)]
+    [HttpPost("{id:guid}/promover-producao")]
+    public async Task<IActionResult> PromoverParaProducao(Guid id, CancellationToken cancellationToken)
+    {
+        await _promoverParaProducao.ExecutarAsync(id, cancellationToken);
+        return NoContent();
+    }
+
     /// <summary>Status do certificado digital da Empresa (sem expor .pfx/senha) — Subject, validade, se está utilizável.</summary>
     [Authorize(Roles = Papeis.Administrador)]
     [HttpGet("{id:guid}/certificado")]
diff -ruN '--exclude=.git' orig/NfseSaaS-master/src/NfseSaaS.Web/Views/Empresas/Index.cshtml NfseSaaS-master/src/NfseSaaS.Web/Views/Empresas/Index.cshtml
--- a/src/NfseSaaS.Web/Views/Empresas/Index.cshtml	2026-09-10 02:39:34.000000000 +0000
+++ b/src/NfseSaaS.Web/Views/Empresas/Index.cshtml	2026-09-10 11:47:12.679970195 +0000
@@ -24,6 +24,7 @@
                     <th>CNPJ</th>
                     <th>Inscrição municipal</th>
                     <th>Município (IBGE)</th>
+                    <th>Ambiente</th>
                     <th>Situação</th>
                     @if (podeEditar)
                     {
diff -ruN '--exclude=.git' orig/NfseSaaS-master/src/NfseSaaS.Web/Views/Shared/_Layout.cshtml NfseSaaS-master/src/NfseSaaS.Web/Views/Shared/_Layout.cshtml
--- a/src/NfseSaaS.Web/Views/Shared/_Layout.cshtml	2026-09-10 02:39:34.000000000 +0000
+++ b/src/NfseSaaS.Web/Views/Shared/_Layout.cshtml	2026-09-10 11:47:10.028083285 +0000
@@ -1,3 +1,4 @@
+@inject Microsoft.Extensions.Options.IOptions<NfseSaaS.Nacional.Options.NfseNacionalOptions> NfseNacionalOptionsAccessor
 @{
     var papelAdministrador = User.IsInRole("Administrador");
     var caminhoAtual = Context.Request.Path.Value?.TrimEnd('/') ?? "";
@@ -5,6 +6,18 @@
     var nomeUsuario = User.Identity?.Name ?? "Usuário";
     var iniciais = string.Concat(nomeUsuario.Split(new[] { '@', '.', ' ' }, StringSplitOptions.RemoveEmptyEntries)
         .Take(2).Select(p => char.ToUpperInvariant(p[0])));
+
+    // Badge global: contra qual endpoint da SEFIN ESTE DEPLOY está
+    // configurado (appsettings NfseNacional:Ambiente) — não confundir
+    // com o ambiente de cada Empresa (esse fica na tela de Empresas).
+    // "ProducaoRestrita" é ambiente de teste do governo, mas o nome não
+    // deixa isso óbvio pra quem só olha o badge — por isso o rótulo e a
+    // cor tratam ela como Homologação (só "Producao" puro é Produção de
+    // verdade).
+    var ambienteSistema = NfseNacionalOptionsAccessor.Value.Ambiente;
+    var sistemaEmProducao = ambienteSistema == "Producao";
+    var rotuloAmbienteSistema = sistemaEmProducao ? "Produção" : "Homologação";
+    var corAmbienteSistema = sistemaEmProducao ? "danger" : "warning";
 }
 <!DOCTYPE html>
 <html lang="pt-br">
@@ -83,6 +96,10 @@
                 </div>
 
                 <div class="d-flex align-items-center gap-2">
+                    <span class="badge text-bg-@corAmbienteSistema" title="Ambiente configurado em NfseNacional:BaseUrl no appsettings deste deploy">
+                        <i class="bi bi-hdd-network"></i> @rotuloAmbienteSistema
+                    </span>
+
                     <button class="btn-tema" id="botaoTema" aria-label="Alternar tema claro/escuro" title="Alternar tema">
                         <i class="bi bi-moon-stars" id="iconeTema"></i>
                     </button>
diff -ruN '--exclude=.git' orig/NfseSaaS-master/src/NfseSaaS.Web/wwwroot/js/paginas/empresas.js NfseSaaS-master/src/NfseSaaS.Web/wwwroot/js/paginas/empresas.js
--- a/src/NfseSaaS.Web/wwwroot/js/paginas/empresas.js	2026-09-10 02:39:34.000000000 +0000
+++ b/src/NfseSaaS.Web/wwwroot/js/paginas/empresas.js	2026-09-10 11:51:57.070734927 +0000
@@ -1,5 +1,15 @@
 const podeEditar = document.getElementById('btn-nova-empresa') !== null;
 
+// Mesmos valores numéricos do enum TipoAmbiente no C# (1 = Producao,
+// 2 = Homologacao) — a API não traduz isso pra string, só serializa o
+// int (mesmo padrão de NfseStatus em nfse.js).
+const AMBIENTE_PRODUCAO = 1;
+const AMBIENTE_HOMOLOGACAO = 2;
+const ROTULOS_AMBIENTE = {
+    [AMBIENTE_PRODUCAO]: { texto: 'Produção', cor: 'danger' },
+    [AMBIENTE_HOMOLOGACAO]: { texto: 'Homologação', cor: 'warning' }
+};
+
 let tabela;
 let modalEmpresa;
 let modalCertificado;
@@ -12,6 +22,13 @@
         { data: 'inscricaoMunicipal' },
         { data: 'codigoMunicipio' },
         {
+            data: 'tipoAmbiente',
+            render: v => {
+                const rotulo = ROTULOS_AMBIENTE[v] ?? { texto: 'Desconhecido', cor: 'secondary' };
+                return `<span class="badge text-bg-${rotulo.cor}">${rotulo.texto}</span>`;
+            }
+        },
+        {
             data: 'ativo',
             render: v => v
                 ? '<span class="badge text-bg-success">Ativa</span>'
@@ -30,6 +47,10 @@
                 <button type="button" class="btn btn-sm btn-outline-info btn-certificado" data-id="${empresa.id}" data-nome="${empresa.razaoSocial}">
                     <i class="bi bi-file-earmark-lock"></i>
                 </button>
+                ${empresa.tipoAmbiente === AMBIENTE_HOMOLOGACAO ? `
+                <button type="button" class="btn btn-sm btn-outline-danger btn-promover-producao" data-id="${empresa.id}" data-nome="${empresa.razaoSocial}" title="Promover para Produção">
+                    <i class="bi bi-rocket-takeoff"></i>
+                </button>` : ''}
                 <button type="button" class="btn btn-sm btn-outline-warning btn-alternar-ativo" data-id="${empresa.id}" data-ativo="${empresa.ativo}">
                     <i class="bi ${empresa.ativo ? 'bi-toggle-on' : 'bi-toggle-off'}"></i>
                 </button>
@@ -64,6 +85,7 @@
 
             if (botao.classList.contains('btn-editar')) await abrirModalEditarEmpresa(id);
             else if (botao.classList.contains('btn-certificado')) await abrirModalCertificado(id, botao.dataset.nome);
+            else if (botao.classList.contains('btn-promover-producao')) await promoverParaProducao(id, botao.dataset.nome);
             else if (botao.classList.contains('btn-alternar-ativo')) await alternarAtivo(id, botao.dataset.ativo === 'true');
             else if (botao.classList.contains('btn-excluir')) await excluirEmpresa(id);
         });
@@ -193,6 +215,22 @@
     } catch (err) {
         mostrarErro(err.message);
     }
+}
+
+async function promoverParaProducao(id, nomeEmpresa) {
+    const confirmado = await confirmarAcao(
+        `Promover "${nomeEmpresa}" para Produção? A partir de agora, toda nota emitida por ela terá efeito fiscal real. Não é possível voltar para Homologação por aqui.`,
+        { titulo: 'Promover para Produção', textoBotao: 'Promover', variante: 'perigo' }
+    );
+    if (!confirmado) return;
+
+    try {
+        await apiFetch(`/api/empresas/${id}/promover-producao`, { method: 'POST' });
+        await carregarEmpresas();
+        mostrarToast(`"${nomeEmpresa}" agora está em Produção.`);
+    } catch (err) {
+        mostrarErro(err.message);
+    }
 }
 
 async function excluirEmpresa(id) {
diff -ruN '--exclude=.git' orig/NfseSaaS-master/tests/NfseSaaS.Tests/DpsBuilderTests.cs NfseSaaS-master/tests/NfseSaaS.Tests/DpsBuilderTests.cs
--- a/tests/NfseSaaS.Tests/DpsBuilderTests.cs	2026-09-10 02:39:34.000000000 +0000
+++ b/tests/NfseSaaS.Tests/DpsBuilderTests.cs	2026-09-10 11:52:27.874676458 +0000
@@ -1,6 +1,4 @@
-using Microsoft.Extensions.Options;
 using NfseSaaS.Nacional.Builders;
-using NfseSaaS.Nacional.Options;
 using NfseSaaS.Tests.TestData;
 using Xunit;
 
@@ -8,16 +6,10 @@
 
 public class DpsBuilderTests
 {
-    private static DpsBuilder CriarBuilder(string ambiente = "ProducaoRestrita")
-    {
-        var options = Options.Create(new NfseNacionalOptions { Ambiente = ambiente });
-        return new DpsBuilder(options);
-    }
-
     [Fact]
     public void DpsBuilder_deve_poder_ser_instanciado()
     {
-        var builder = CriarBuilder();
+        var builder = new DpsBuilder();
 
         Assert.NotNull(builder);
     }
@@ -25,7 +17,7 @@
     [Fact]
     public void Construir_deve_gerar_xml_contendo_o_Id_do_infDPS_retornado()
     {
-        var builder = CriarBuilder();
+        var builder = new DpsBuilder();
         var request = DpsRequestFactory.CriarRequestDeTeste();
 
         var (xml, infDpsId) = builder.Construir(request);
@@ -37,7 +29,7 @@
     [Fact]
     public void Construir_deve_incluir_os_dados_de_prestador_e_tomador_informados()
     {
-        var builder = CriarBuilder();
+        var builder = new DpsBuilder();
         var request = DpsRequestFactory.CriarRequestDeTeste();
 
         var (xml, _) = builder.Construir(request);
@@ -47,17 +39,36 @@
         Assert.Contains(request.Tomador.Nome, xml);
     }
 
+    /// <summary>
+    /// tpAmb agora vem pronto de fora (request.TpAmb, decidido por
+    /// Empresa.TipoAmbiente em EmitirNfseUseCase) — o DpsBuilder só
+    /// escreve o que recebeu, não decide mais nada a partir de
+    /// appsettings/NfseNacionalOptions. Ver comentário de
+    /// DpsRequest.TpAmb sobre por que essa tradução não pode acontecer
+    /// dentro do módulo Nacional.
+    /// </summary>
     [Theory]
-    [InlineData("Producao", "<tpAmb>1</tpAmb>")]
-    [InlineData("Homologacao", "<tpAmb>2</tpAmb>")]
-    [InlineData("ProducaoRestrita", "<tpAmb>2</tpAmb>")]
-    public void Construir_deve_mapear_o_ambiente_configurado_para_o_tpAmb_correto(string ambiente, string tpAmbEsperado)
+    [InlineData("1", "<tpAmb>1</tpAmb>")]
+    [InlineData("2", "<tpAmb>2</tpAmb>")]
+    public void Construir_deve_escrever_o_tpAmb_recebido_no_request(string tpAmb, string tpAmbEsperado)
     {
-        var builder = CriarBuilder(ambiente);
-        var request = DpsRequestFactory.CriarRequestDeTeste();
+        var builder = new DpsBuilder();
+        var request = DpsRequestFactory.CriarRequestDeTeste() with { TpAmb = tpAmb };
 
         var (xml, _) = builder.Construir(request);
 
         Assert.Contains(tpAmbEsperado, xml);
     }
-}
\ No newline at end of file
+
+    [Theory]
+    [InlineData("3")]
+    [InlineData("Homologacao")]
+    [InlineData("")]
+    public void Construir_deve_rejeitar_tpAmb_fora_de_1_ou_2(string tpAmbInvalido)
+    {
+        var builder = new DpsBuilder();
+        var request = DpsRequestFactory.CriarRequestDeTeste() with { TpAmb = tpAmbInvalido };
+
+        Assert.Throws<ArgumentException>(() => builder.Construir(request));
+    }
+}
diff -ruN '--exclude=.git' orig/NfseSaaS-master/tests/NfseSaaS.Tests/DpsSignerTests.cs NfseSaaS-master/tests/NfseSaaS.Tests/DpsSignerTests.cs
--- a/tests/NfseSaaS.Tests/DpsSignerTests.cs	2026-09-10 02:39:34.000000000 +0000
+++ b/tests/NfseSaaS.Tests/DpsSignerTests.cs	2026-09-10 11:53:31.593956332 +0000
@@ -1,8 +1,6 @@
-﻿using System.Security.Cryptography;
+using System.Security.Cryptography;
 using System.Security.Cryptography.X509Certificates;
-using Microsoft.Extensions.Options;
 using NfseSaaS.Nacional.Builders;
-using NfseSaaS.Nacional.Options;
 using NfseSaaS.Nacional.Signing;
 using NfseSaaS.Tests.TestData;
 using Xunit;
@@ -36,8 +34,7 @@
     [Fact]
     public void Assinar_deve_incluir_um_elemento_Signature_referenciando_o_infDpsId()
     {
-        var options = Options.Create(new NfseNacionalOptions { Ambiente = "ProducaoRestrita" });
-        var builder = new DpsBuilder(options);
+        var builder = new DpsBuilder();
         var signer = new DpsSigner();
 
         var request = DpsRequestFactory.CriarRequestDeTeste();
@@ -53,8 +50,7 @@
     [Fact]
     public void Assinar_sem_chave_privada_deve_lancar_NfseCertificateException()
     {
-        var options = Options.Create(new NfseNacionalOptions { Ambiente = "ProducaoRestrita" });
-        var builder = new DpsBuilder(options);
+        var builder = new DpsBuilder();
         var signer = new DpsSigner();
 
         var request = DpsRequestFactory.CriarRequestDeTeste();
diff -ruN '--exclude=.git' orig/NfseSaaS-master/tests/NfseSaaS.Tests/TestData/DpsRequestFactory.cs NfseSaaS-master/tests/NfseSaaS.Tests/TestData/DpsRequestFactory.cs
--- a/tests/NfseSaaS.Tests/TestData/DpsRequestFactory.cs	2026-09-10 02:39:34.000000000 +0000
+++ b/tests/NfseSaaS.Tests/TestData/DpsRequestFactory.cs	2026-09-10 11:52:01.700663630 +0000
@@ -35,5 +35,6 @@
         Valor: 10.00m,
         CodigoTributacaoNacional: "010701",
         CodigoNbs: "115013000",
-        DescricaoServico: "Teste");
+        DescricaoServico: "Teste",
+        TpAmb: "2");
 }
\ No newline at end of file
diff -ruN '--exclude=.git' orig/NfseSaaS-master/tools/NfseSaaS.EmitirTeste/Program.cs NfseSaaS-master/tools/NfseSaaS.EmitirTeste/Program.cs
--- a/tools/NfseSaaS.EmitirTeste/Program.cs	2026-09-10 02:39:34.000000000 +0000
+++ b/tools/NfseSaaS.EmitirTeste/Program.cs	2026-09-10 11:53:44.362810351 +0000
@@ -111,7 +111,12 @@
     Valor: 10.00m,
     CodigoTributacaoNacional: "010701",
     CodigoNbs: "115013000",
-    DescricaoServico: "TESTE DE EMISSAO NFS-E VIA NFSESAAS - ARQUITETURA DEFINITIVA");
+    DescricaoServico: "TESTE DE EMISSAO NFS-E VIA NFSESAAS - ARQUITETURA DEFINITIVA",
+    // DpsBuilder não lê mais "ambiente" do appsettings/IOptions — quem
+    // decide o tpAmb agora é sempre o chamador. Nesta ferramenta manual,
+    // isso é o argumento de linha de comando [ambiente] (mesma regra que
+    // já era usada: só "Producao" vira tpAmb=1, tudo mais é teste).
+    TpAmb: ambiente == "Producao" ? "1" : "2");
 
 Console.WriteLine("==========================================");
 Console.WriteLine(" NfseSaaS - Emissão de teste (arquitetura definitiva)");
