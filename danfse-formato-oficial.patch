diff --git a/src/NfseSaaS.Infrastructure/Danfse/DanfseDocument.cs b/src/NfseSaaS.Infrastructure/Danfse/DanfseDocument.cs
index 8858769..898ee59 100644
--- a/src/NfseSaaS.Infrastructure/Danfse/DanfseDocument.cs
+++ b/src/NfseSaaS.Infrastructure/Danfse/DanfseDocument.cs
@@ -184,15 +184,22 @@ internal sealed class DanfseDocument : IDocument
     }
 
     /// <summary>
-    /// Este SaaS não coleta um "destinatário da operação" distinto do
-    /// tomador — não há esse conceito na tela de emissão. Nota 3 da NT 008
-    /// cobre exatamente esse caso: informar a frase padrão em vez de
-    /// duplicar os dados do tomador.
+    /// Este SaaS não coleta um "destinatário da operação" nem um
+    /// "intermediário da operação" distintos do tomador — não há esse
+    /// conceito na tela de emissão. Texto e formatação (2 linhas
+    /// separadas, sem título de seção individual pra cada uma) copiados
+    /// do DANFSe oficial baixado do portal nacional da NFS-e, pra ficar
+    /// visualmente idêntico ao que o cliente já está acostumado a ver
+    /// lá — dá mais credibilidade que uma frase equivalente mas
+    /// redigida diferente.
     /// </summary>
     private void BlocoDestinatario(IContainer container)
     {
-        BlocoComTitulo(container, "DESTINATÁRIO DA OPERAÇÃO", corpo =>
-            corpo.Item().Text("O DESTINATÁRIO É O PRÓPRIO TOMADOR/ADQUIRENTE DA OPERAÇÃO").FontSize(7));
+        container.Column(coluna =>
+        {
+            coluna.Item().Text("DESTINATÁRIO DA OPERAÇÃO NÃO IDENTIFICADO NA NFS-e").FontSize(7);
+            coluna.Item().Text("INTERMEDIÁRIO DA OPERAÇÃO NÃO IDENTIFICADO NA NFS-e").FontSize(7);
+        });
     }
 
     private void BlocoServico(IContainer container)
diff --git a/src/NfseSaaS.Infrastructure/Danfse/DanfseXmlDados.cs b/src/NfseSaaS.Infrastructure/Danfse/DanfseXmlDados.cs
index 3b24310..8562206 100644
--- a/src/NfseSaaS.Infrastructure/Danfse/DanfseXmlDados.cs
+++ b/src/NfseSaaS.Infrastructure/Danfse/DanfseXmlDados.cs
@@ -67,7 +67,17 @@ internal sealed class DanfseXmlDados
 
     public string NumeroDps() => Texto("//n:NFSe/n:infNFSe/n:DPS/n:infDPS/n:nDPS") ?? "-";
 
-    public string SerieDps() => Texto("//n:NFSe/n:infNFSe/n:DPS/n:infDPS/n:serie") ?? "-";
+    /// <summary>
+    /// 5 dígitos com zero à esquerda — puramente de exibição, copiado do
+    /// formato do DANFSe oficial do portal nacional (o valor real em
+    /// n:serie continua sem padding; PadLeft aqui não altera o XML,
+    /// só como aparece na tela/PDF).
+    /// </summary>
+    public string SerieDps()
+    {
+        var serie = Texto("//n:NFSe/n:infNFSe/n:DPS/n:infDPS/n:serie");
+        return serie is null ? "-" : serie.PadLeft(5, '0');
+    }
 
     public string DataHoraEmissaoDps() => FormatarDataHora(Texto("//n:NFSe/n:infNFSe/n:DPS/n:infDPS/n:dhEmi"));
 
